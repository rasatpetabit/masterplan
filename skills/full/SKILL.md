---
name: full
description: "Full brainstorm → spec → plan → execute kickoff in one session (B0 → B1 → B2 → B3 → C)."
---

<!-- masterplan verb: full -->

Invoke the masterplan **`full`** workflow. Load `commands/masterplan.md` from the plugin root and proceed with `requested_verb = "full"`. Any additional text in this invocation is the project topic or feature description.

Find `commands/masterplan.md` using the resolution order documented in `skills/masterplan/SKILL.md`.
