---
name: status
description: "Read-only situation report: active bundle, current phase, pending gates, and suggested next action."
---

<!-- masterplan verb: status -->

Invoke the masterplan **`status`** workflow. Load `commands/masterplan.md` from the plugin root and proceed with `requested_verb = "status"`. This is a read-only subroutine — no state is modified.

Find `commands/masterplan.md` using the resolution order documented in `skills/masterplan/SKILL.md`.
