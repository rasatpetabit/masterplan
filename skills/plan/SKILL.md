---
name: plan
description: "Create or refine an execution plan from a spec; halts at the plan approval gate (B3)."
---

<!-- masterplan verb: plan -->

Invoke the masterplan **`plan`** workflow. Load `commands/masterplan.md` from the plugin root and proceed with `requested_verb = "plan"`. Any additional text in this invocation is the spec path or topic to plan.

Find `commands/masterplan.md` using the resolution order documented in `skills/masterplan/SKILL.md`.
