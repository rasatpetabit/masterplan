---
name: doctor
description: "Run all 52 masterplan self-host lint checks and optionally apply safe fixes."
---

<!-- masterplan verb: doctor -->

Invoke the masterplan **`doctor`** workflow. Load `commands/masterplan.md` from the plugin root and proceed with `requested_verb = "doctor"`. Pass `--fix` to apply safe auto-fixes; omit for a read-only audit.

Find `commands/masterplan.md` using the resolution order documented in `skills/masterplan/SKILL.md`.
