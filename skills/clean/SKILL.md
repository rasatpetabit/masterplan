---
name: clean
description: "Archive completed bundles and prune stale state files from docs/masterplan/."
---

<!-- masterplan verb: clean -->

Invoke the masterplan **`clean`** workflow. Load `commands/masterplan.md` from the plugin root and proceed with `requested_verb = "clean"`. Archives completed bundles and removes orphaned state files per the retention policy in `commands/masterplan.md`.

Find `commands/masterplan.md` using the resolution order documented in `skills/masterplan/SKILL.md`.
