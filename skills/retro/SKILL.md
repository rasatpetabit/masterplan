---
name: retro
description: "Generate a retrospective for a completed or recently finished bundle."
---

<!-- masterplan verb: retro -->

Invoke the masterplan **`retro`** workflow. Load `commands/masterplan.md` from the plugin root and proceed with `requested_verb = "retro"`. Any additional text in this invocation is a `state.yml` path or bundle slug to retro.

Find `commands/masterplan.md` using the resolution order documented in `skills/masterplan/SKILL.md`.
