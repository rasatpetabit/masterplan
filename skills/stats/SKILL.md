---
name: stats
description: "Telemetry roll-up: bundle counts, task completion rates, and timing across all run bundles in this repo."
---

<!-- masterplan verb: stats -->

Invoke the masterplan **`stats`** workflow. Load `commands/masterplan.md` from the plugin root and proceed with `requested_verb = "stats"`. This is a read-only subroutine that aggregates telemetry from `events.jsonl` files.

Find `commands/masterplan.md` using the resolution order documented in `skills/masterplan/SKILL.md`.
