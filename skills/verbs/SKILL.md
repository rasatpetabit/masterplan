---
name: verbs
description: "Display the /masterplan verb quick-reference — what each verb does and where it routes."
---

<!-- masterplan verb-reference -->

Display the masterplan verbs quick-reference. Find and Read `docs/verbs.md` from the plugin root using the same resolution order documented in `skills/masterplan/SKILL.md` (substitute `docs/verbs.md` for `commands/masterplan.md`). Print its contents verbatim.

Then close with an `AskUserQuestion` offering 2–4 options, for example: start a `full` kickoff, resume with `execute`, run `doctor`, or "done — no action needed".
