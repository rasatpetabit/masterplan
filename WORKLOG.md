# WORKLOG

## 2026-06-26 — pre-execute gate enforcement: triage hardening (receipt-binding, confined paths, fail-closed, ordering)

Applied the fix pass for the prior cross-vendor triage of the two PRE-EXECUTE adversary-review gates (`gate-review-triage.md`, hash `48fea81c…`). Change set: `lib/gate-review.mjs`, `bin/masterplan.mjs` (gate hunks only — the `rebase-paths` hunk in that file is the separate user-owned stream above), `commands/masterplan.md` §3b, `test/gate-review.test.mjs`, `test/bin-masterplan.test.mjs`, `test/coord-writer.test.mjs`. Why: the gate code was written and reviewed in a prior session that ended before the findings were folded in; this is the one coherent fix pass.

**What landed (by triage finding):**
- **P1 record-forgery → structured receipt binding.** New pure `validateGateReceipt(receipt, {gate, hash, artifacts})` in `lib/gate-review.mjs`. `record-gate-review --status=done` now REQUIRES `--receipt=<json|file>` that echoes the recomputed hash + artifact set and carries real lane provenance (`dispatch_id`/`provider`/`model` non-empty, `output_tokens`/`completion_tokens` finite >0, non-empty `digest`/`ts`); `--status=skipped` REQUIRES non-empty `--reason` AND a readable non-empty `--digest-file`. Honest ceiling: same agent runs lane+writes record, so this raises forgery friction, not cryptographic proof.
- **P1-B non-canonical/unconfined paths → single resolver + realpath confinement.** New `resolveGateArtifacts({gate,statePath,state,flags,op})` is the SOLE artifact source for enforce/record/gate-hash (so a record and its guard can never hash different bytes). Every candidate is `realpathSync`'d and confined to the bundle dir (`rel.startsWith('..')||isAbsolute` → die 1) — defeats symlink/`..` escape on ALL ops regardless of `--force`. `set-phase` ignores path flags (canonical paths); `load-plan`/`record`/`gate-hash` honor `--plan-index`/`--plan-md` (operation target, still confined).
- **P2 missing-artifact-hashed-as-empty → fail-closed.** `Buffer.alloc(0)` for a missing/unparseable artifact is gone; a missing required artifact or unparseable `plan.index.json` now dies(1) on every path.
- **P3a clobber-after-validate → hoisted.** `load-plan` rejects a populated bundle BEFORE reading/validating/gating the index; index read+parsed ONCE and reused (validate+gate+stamp+materialize), the parsed object passed to the gate as `prereadIndex` to close the read→hash TOCTOU.
- **P3b out()+exit truncation → `fs.writeSync(1, …)` + `process.exit(3)`.**
- **--force audit:** a `--force` bypass now appends a `<gate>_gate_bypassed` event (never silent).
- New read-only `mp gate-hash` subcommand emits `{hash, artifacts}` so the shell/tests learn what to echo into a receipt; `gate-review-status` migrated to the same resolver. §3b prose rewritten to the receipt/skip flow. Hash key format changed (relName-based, top-level-sorted normalized index) — internally consistent; no production bundles depend on the old key.

**Verification:** full `node --test` **1002/1003 pass** (the 1 fail is the pre-existing `agents/mp-implementer.md` tools-regex, untouched); `node bin/doctor.mjs` exit 0, 0 errors. Tests: `gate-review.test.mjs` +8 (validateGateReceipt unit), `bin-masterplan.test.mjs` +13 (exit-3+op+state-unchanged for both gates, spec-edit re-arm, plan_hash/generated_at-only edit does NOT re-arm, skip-evidence required, fail-soft skip satisfies, fabricated-done rejected, fail-closed missing artifact, `--plan-md`/symlink escape refused, clobber-before-validate, `--force` audit event); `passGate` helper in both bin+coord-writer rewritten to mint a real receipt via `gate-hash` and stub the now-required artifacts; coord-writer "missing plan.md" test converted to `--force` (the gate is now fail-closed on plan.md).

**Caveat — cross-vendor re-review BLOCKED:** the authoritative gpt-5.5 adversarial pass on the merged diff could not run — the skynet gateway is down (all 4 endpoints 502/timeout; `dispatch-cross-review` alias not routable), the same outage `doctor adversary-lane-health` WARNs. The diff is staged at `scratchpad/gate-impl.diff` for re-review when the lane recovers. Left UNCOMMITTED for user review.

## 2026-06-25 — doctor `adversary-lane-health` live fix + `mp rebase-paths` + audit-cleanup

An audit pass landed a real defect fix in `lib/doctor/adversary-lane-health.mjs` (the backend-health probe mis-invoked `agent-dispatch health --class adversary`; the verb takes a POSITIONAL backend, so the catch never fired and the lane silently read healthy) and a set of doc-hygiene edits (de-dup hardcoded `gpt-5.5`/`skynet-local` → defer to `agent-dispatch digest`; `lib/routing.mjs`→`lib/dispatch/routing.mjs` path fix; `codex-auth` comment reframed as informational-only). The audit's self-report was partly incorrect and its fix was unreachable in the live state, so this entry records what actually landed after correction.

**Audit deviations corrected:**
- The audit reported "Suite 914 pass / 2 fail" — actual was **936/1** (single pre-existing `agents/mp-implementer.md` tools-regex; the cited second `plan-merge.test.mjs renderPlanHtml` failure does not exist — that test passes 22/22 and no `renderPlanHtml` symbol is referenced anywhere). Recorded so a future audit can't cite stale counts.
- The audit reported "WORKLOG entry added" — none was. This is that entry.
- The audit reported end-to-end verification: "with the gateway down, the doctor correctly WARNs 'dispatch-gateway reports unhealthy' instead of falsely passing." Not reproduced. With the gateway down `agent-dispatch resolve --class adversary` flaps between exit 1 (threw, `chain_exhausted` on stderr) and exit 0 with a bare `chain_exhausted` token on stdout — EITHER way the audit's JSON-parse/backend-probe branch was unreachable in the live state, and the doctor emitted the same generic "resolve failed" WARN the OLD code already produced. The audit's verification did not actually run what it cited.

**The corrected fix** (`lib/doctor/adversary-lane-health.mjs`): the audit's positional-backend correction is sound and kept (it's the right call when `resolve` SUCCEEDS with JSON), AND a fallback path is added: when `resolve` throws OR emits a bare failure token (`chain_exhausted`, `escalate`, `budget_breach`, `no_route`, `unresolved`) OR returns JSON with no `backend` OR a JSON decision/status/reason of one of those failure tokens, fall back to the configured class chain — read via `agent-dispatch where` → `policy/dispatch-policy.jsonc` — and probe each configured backend's `health <backend>` directly, so the WARN can name the sick backend. Three refactored helpers: pure `parseResolveOutput(rawOut)` and `parseConfiguredBackends(policyText)` (exported for unit tests), plus the fs/child_process wrappers `agentDispatchRoot()` / `readConfiguredBackends(repoRoot)` / `probeBackendHealth(backend)`. Probe-seam contract extended with optional `unhealthyBackends: string[]|null`. **Live-verified end-to-end** (this time, for real): with the gateway confirmed down (`agent-dispatch health dispatch-gateway` → `healthy:false`, exit 0), `node bin/doctor.mjs` now emits, across 6 consecutive runs, either `WARN adversary-lane-health: adversary lane resolves (dispatch-adversary) but its backend is unhealthy: dispatch-gateway reports unhealthy …` (when resolve briefly returned JSON) OR `WARN adversary-lane-health: adversary lane backend unhealthy: dispatch-gateway reports unhealthy (\`resolve --class adversary\` exhausted) — review may degrade to inconclusive` (when resolve threw/returned a failure token). Both name `dispatch-gateway reports unhealthy` regardless of the flaky resolve shape. The advisory invariant (never ERROR) is preserved. `test/doctor.test.mjs` +11 (resolve-throws-with-configured-backend → named-backend WARN; resolve-throws-no-backend → generic WARN; 9 pure-helper tests covering parseResolveOutput's failure shapes + parseConfiguredBackends JSONC tolerance); injected-probe contract unchanged so existing tests stay green.

**New `mp rebase-paths` verb** (`bin/masterplan.mjs` + `lib/bundle.mjs`): the CD-7-compliant single-writer for the bundle's absolute path fields (`spec_path`/`plan_path`/`plan_index_path`/`worktree`) after a repo relocation — the ONLY writer for these fields besides `seed`. The 2026-06-22 user-owned hand-edit of `state.yml`+`.bak` path rebrands (`/srv/dev/masterplan/...`→`/srv/dev/ras/masterplan/...`, left sitting in the dirty tree as a separate workstream) was a CD-7 violation with no mp-native alternative; this verb closes that gap. Pure transform `rebasePaths(state, fromRoot, toRoot)` (only leading-prefix matches are rewritten; identical roots is a no-op; relative roots throw; re-running with the same `from` is idempotent). `test/bundle.test.mjs` +1 (pure helper, idempotency, validation); `test/bin-masterplan.test.mjs` +3 (write+count, idempotent re-rebase, relative-root rejection). **Applied** to the three live state.yml files (codex-review-issues: 4 fields rebased; finish-flow-hardening: 3; github-coordination: 3). The `docs/masterplan/cc3-visibility/state.yml.v5.1.bak` artifact is a schema-5.1 legacy backup, NOT live state, and `loadForWrite` correctly refuses it; its stale `/srv/dev/masterplan/...` path is left as-is (frozen snapshot).

**Cleanup:** removed the 3 orphan `.owner.hb.*` heartbeat files in `docs/masterplan/codex-review-issues/` (the `owner-sentinel` doctor's "safe to remove" path — Guard D volatile artifacts from a 2026-06-18 run with no `.owner.lock`); `owner-sentinel` now PASSes.

**Audit's kept work (verified correct):** doc hygiene — `skynet-local` purged from lib/docs; `gpt-5.5`/`skynet gateway` hardcoded refs de-duped to "see `agent-dispatch digest`" in README/install/development/agents/mp-adversarial-reviewer; `lib/routing.mjs`→`lib/dispatch/routing.mjs` in `plan-annotations.md` (old path nonexistent, new path correct); `codex-auth.mjs` comment reframed as WARN-only-and-informational (accurate — the check is WARN-only, never ERROR, so it never gates a run); `adsp-adapter.mjs` "BUILT + TESTED BUT NOT YET WIRED" status note + `plan-annotations.md` cross-link (verified: zero non-test callers of `escalateCrossReview`/`revertCrossReview`/`dispatchTask`). The audit's cross-repo sync claim holds: `agent-dispatch compile --target srv-dev-agents-routing --dry-run` → `changed:false`, and the §routing managed-block hash (`6d8307801e22...`) matches identically between `masterplan/AGENTS.md` and `agent-dispatch/AGENTS.md`.

**Hardcoded-account path resolved:** the audit's `escalateCrossReview` hardcoded `/home/ras/.claude/plugins/marketplaces/rasatpetabit-masterplan/bin/masterplan.mjs` — account-specific, broke on `ras@*` accounts. Replaced with `resolveMasterplanBin` (new helper in `lib/paths.mjs`, the single source of truth for filesystem locations): portable across accounts via `os.homedir()` + `resolveConfigDir` (honors `$CLAUDE_CONFIG_DIR`), with `$MP_BIN` (absolute bin path) and `$MP_MARKETPLACE_DIR` overrides for edge cases; never a hardcoded `/home/<user>` literal. `test/paths.test.mjs` +7 (defaults-portable, `$CLAUDE_CONFIG_DIR`, `$MP_BIN` absolute+relative, `$MP_MARKETPLACE_DIR` absolute+relative, precedence, blank-ignored); `test/adsp-adapter.test.mjs` +1 (`escalateCrossReview` without `opts.masterplanBin` resolves via `$MP_BIN`, no `/home/ras` leak). Verified on-host: `resolveMasterplanBin()` → the correct `grojas`-account path via homeDir, and a `ras` account would resolve to its own `~/.claude/...`. The bin-exists probe is deliberately omitted — `spawn()` surfaces ENOENT loudly if the install is absent, which is the right failure mode for an unwired escalation seam.

**Remaining caveats:** The cross-vendor adversary review ran DEGRADED today (1 of 2–3 requested reviewers — the gpt-5.5 gateway is the same flap the doctor now correctly surfaces). The pre-existing `index-staleness`/`scalar-cap` WARNs (`qctl-implementer-backend` plan_hash stale; `finish-flow-hardening` topic overflow) are unrelated user-owned tech debt, not touched here. **Suite 958/959** (the 1 fail is the pre-existing `agents/mp-implementer.md` tools-regex). Doctor exit 0.

## 2026-06-25 — `doctor --fix` clears stale worktree pointers (issue #7)

Closed the one real gap left by #7 after the v8 redesign already shipped `mp set-worktree-disposition` + the sweep reconciler: `doctor --fix` had **no** autofix for a bundle whose worktree was reclaimed without `mp finish`, so `worktree-integrity.check()` ERROR'd on it every scan — an ever-growing noise floor (the report cited 26 such WARNs, 25 historical pre-rename `/home/ras/...` cases). Added a conservative `fix()` to `lib/doctor/worktree-integrity.mjs` (auto-discovered by `runFixes`; no wiring). **Design correction (surfaced mid-build):** my approved predicate keyed on `status === 'complete'`, but `complete` is **not a valid bundle status** (`VALID_STATUS = ['in-progress', 'archived']`; line 241 calls it a rejected typo) — the only non-archived status is `in-progress`, so a status gate would be vacuous. Re-keyed the discriminator on **disk existence**: retire iff `worktree` set ∧ unregistered in git ∧ `!fs.existsSync` ∧ not archived ∧ not already a skip-disposition → record `removed_after_merge` (the check's line-125 skip then clears BOTH the worktree and branch ERROR; path preserved as a reversible memento, no field nulled). `!fs.existsSync` **is** the Codex-BLOCKER line: the protected `manual`/active-unregistered case requires the dir to exist (so `.git` can be inspected), so a vanished path can never be that live reference; on-disk strays are left for the operator. Fix set is a strict subset of check's ERROR set; idempotent (2nd run no-ops via the disposition skip). **Empirically settled the #7 payload question:** fix() clears a legacy schema-5.1 bundle's ERRORs with **zero field loss** (regression test pins it) — only side effect is a one-time cosmetic re-quote of the quoted-string `schema_version` that the documented manual remedy `mp set-worktree-disposition` produces identically (no classification change, doesn't compound). So this resolves the historical noise, not just future accrual. TDD RED→GREEN; 4 new tests (retire+idempotent, on-disk-stray-untouched, archived-skipped, legacy-no-field-loss). **Verified:** fix tests 4/4; full suite 946/947 pass (the 1 fail is the pre-existing `agents/mp-implementer.md` `mcp__skynet__*` tools-regex — proven pre-existing by stash-and-rerun, untouched here); doctor exit 0. **Cross-vendor adversarial pass PENDING:** skynet dispatch-gateway down at author time (`health_status` → `dispatch-gateway: healthy:false`), so the diff got the sanctioned same-vendor `advisor()` fallback instead; re-run `agent-dispatch review --class adversary` on the merged diff when the gateway recovers. Worktree `.worktrees/issue-7` off `origin/main`; pre-existing dirty files on the concurrent session's local `main` untouched.

## 2026-06-25 — add rendered `plan.html` artifact + `render` verb (planf3 cross-pollination)

Implemented (via `/plan`, plan-approved) the one cross-pollination from the planf3↔masterplan comparison: a rendered HTML plan view, masterplan's biggest UX gap vs planf3. **Key decision: SVG-only, no AI images.** The user first picked planf3-style `gpt-image-2` diagrams, then reversed to SVG after I surfaced (prompted by the adversary pass) that the only image path bypasses the agent-dispatch gateway — which has *no image lane* — dragging in secrets/network/cost/non-determinism. SVG-only keeps every masterplan invariant (deterministic, self-contained, zero-dep, headless-safe, in-policy). **Design:** pure `renderPlanHtml(index, meta)` in `lib/plan-merge.mjs` mirroring `renderPlanMd` — additive (`plan.md` stays canonical), HTML-escapes all interpolated fields, wave-banded inline SVG that is explicitly **not** a dependency graph (the merge drops `key`/`deps`, so edges would be fabricated). Two fs-only entry points: best-effort auto-emit in the `load-plan` case (swallowed on failure, before the atomic `writeState`; verified `load-plan` is the common hook for both serial+parallel planning paths) and a new read-only `render-plan` subcommand backing the `render` verb (live status from `state.tasks`, never writes state). `bundleArtifacts()` gains `planHtml`. **Cross-vendor adversarial review (gpt-5.5)** on the plan caught two real blockers folded in before coding: HTML-escaping (Markdown→HTML context shift) and the dependency-graph misnomer; also drove the read-only/determinism/minimal-fixture test hardening. Built TDD wave-by-wave (RED→GREEN each). Verb wired across all 6 sync surfaces + `RESERVED_VERBS`. **Verified:** plan-merge 27/27, paths 15/15, bin-masterplan 129/129, publish-hygiene 18/18; doctor exit 0. Full-suite run + e2e pending at close. Left uncommitted for user; on `main` so will branch before any commit. Pre-existing dirty files untouched.

## 2026-06-25 — audit masterplan↔agent-dispatch drift; fix doctor health-probe + doc hygiene

Audit (via `/plan`) of how masterplan consumes agent-dispatch, looking for drift/conflict/duplication. **Headline:** integration is largely clean — the codex→adversary review migration is synchronized, and the live wire contract (`review`/`resolve` CLI, `dispatch_task` MCP, `§routing` managed blocks) matches the provider. **One real defect found** (and the first-pass "no drift" conclusion was overturned by a cross-vendor adversary pass that pushed me to smoke-test the live CLI): `lib/doctor/adversary-lane-health.mjs` mis-invoked `agent-dispatch health --class adversary` — the `health` verb takes a POSITIONAL backend, not `--class`, so that probe always errored and the `catch` left `healthy:true`, making step-3 backend-health a **silent no-op** (never detected an unhealthy lane). **Fix:** parse the backend from `resolve`'s JSON decision record (`.backend`/`.provider`) and probe `agent-dispatch health <backend>`, parsing `{healthy:bool}`; legacy non-JSON outputs fall back to the old regex. Verified end-to-end: with the gateway down right now, the doctor now correctly WARNs "dispatch-gateway reports unhealthy" instead of falsely passing. Also fixed the WARN's `fix:` hint (was naming the same broken `--class` command). **Doc hygiene:** dropped retired alias `skynet-local` (`agents/mp-adversarial-reviewer.md`); de-duplicated hardcoded reviewer model `gpt-5.5`/"skynet gateway" → "resolved by agent-dispatch; see `agent-dispatch digest`" (`README.md`, `docs/install.md`, `docs/development.md`) per the no-copy-routing boundary rule; removed a stale `codex-plugin-presence` doctor reference (`bin/masterplan.mjs:255`); fixed a stale `lib/routing.mjs`→`lib/dispatch/routing.mjs` path and cross-linked adsp-v1 in `docs/conventions/plan-annotations.md`. **adsp-v1 de-risk:** added a "BUILT + TESTED BUT NOT WIRED" status note atop `lib/dispatch/adsp-adapter.mjs` (zero production callers; live path is mp-implementer inline) + flagged its hardcoded plugin masterplan.mjs path as a wire-time TODO — kept staged, NOT wired (deliberate future-tier per WORKLOG 2026-06-22). **Decision deviation:** the plan defaulted to *retiring* the `codex-auth` doctor check (stale "review path" rationale), but my search tooling (RTK/Headroom proxy) was corrupting the exact `codex-auth`↔`ln` identifiers needed to safely drive a multi-file delete (file + 4 test sites + fixtures + README) — so I took the plan's lower-risk alternative and **repointed** its rationale text instead (it degrades safe: WARN/SKIP, SKIPs when codex absent). codex-auth still PASS. **Verified:** suite 914 pass / 2 fail — both pre-existing & user-owned (the known `mp-implementer` tools-regex + a dirty `test/plan-merge.test.mjs` red test importing an unimplemented `renderPlanHtml`, neither touched by this work); doctor exit 0; adsp-v1 still has zero non-test callers. Cross-vendor adversary review ran **degraded** (gateway down — 1 of 2-3 reviewers). Left uncommitted for user; pre-existing dirty files (`test/plan-merge.test.mjs`, `test/adsp-adapter.test.mjs`, `state.yml`/`.bak`, `.owner.hb.*`) untouched.

## 2026-06-25 — thin AGENTS.md (171→76 lines); contributor discipline → new docs/development.md

User asked for thin AGENTS.md/CLAUDE.md that *point to* operational docs rather than restating them. CLAUDE.md was already a thin shim; all work was on AGENTS.md. **Key decision (user steer):** the inline guardrail prose that merely restated CD-3 (verify-before-done) / CD-7 (durable state) / CD-9 (AUQ) is **generic agent policy already carried by the global + agent-dispatch settings** — per the host's AGENTS-vs-CLAUDE boundary rule, generic policy is NOT restated in a repo file, so those were **dropped, not relocated**. What stayed repo-local: the canonical CD-N *bodies* (already isolated in `docs/conventions/cd-rules.md`, untouched — live code references the IDs) and the genuinely masterplan-specific discipline. **Moves:** the ~50-line five-layer-architecture narrative → compressed to a 1-paragraph summary pointing at the existing `docs/internals.md` (which already holds the full L0–L4 overview, so no new `docs/architecture.md`); the 5 anti-patterns + operating-principles + build/test block → **new `docs/development.md`** (single-writer state, dispatch-don't-work-in-shell, sync'd verb/doctor surfaces, cross-vendor fresh-eyes review, finish-flow durability). AGENTS.md now = central-pointer sentinel + thin "what this is" + the kept "Where to read first" nav table (added a development.md row) + a 2-bullet "Discipline & development" pointer block + the routing sentinel. **Preserved verbatim:** `agentic-dispatch:central-pointer`, the `agent-dispatch:begin/end` routing block (diff-verified byte-identical to HEAD), and CLAUDE.md's shim/notes markers. Repointed CLAUDE.md's stale "canonical in AGENTS.md" line (below the notes barrier) and added a `development.md` bullet to internals.md Cross-cutting References. **Verified:** suite 937/936 (the 1 fail is the pre-existing `mp-implementer` tools-regex, unrelated), doctor exit 0, all 21 doc links resolve mechanically, sentinels intact. No test/doctor/hygiene reads AGENTS.md content (verbs parse from `commands/masterplan.md`), so nothing mechanical was at risk. Cross-vendor adversary review lane was DOWN (skynet gpt-5.5 gateway unhealthy) and the fable subagent fallback was unavailable + opus-subagent budget 0 — so the fresh-eyes pass was done by the author over the full diff + a deterministic link/sentinel checker rather than an independent model. Uncommitted (left for user); pre-existing dirty files (state.yml/.bak, adsp-adapter, .owner.hb.*) untouched.
## 2026-06-22 — retire `codex:codex-rescue` rescue subsystem + reconcile Track B codex→adversary docs

Part of the cross-repo codex-cli retirement (agent-dispatch Track A + masterplan Track B landed as one coordinated commit set). Track B had already repointed whole-branch review from shelling to Codex → `agent-dispatch review --class adversary` (cross-vendor gpt-5.5). This change finishes it. **Rescue subsystem removed:** `suppressRescue` deleted from `lib/dispatch/host.mjs` (+ `index.mjs` re-export, `bin/masterplan.mjs` import and `detect-host` output field — now bare `{isCodex, reasons}`). **Stale review-gate removed:** `finishStep`'s `codexSuppressed` param + its three uses (skipReason / run-gate / reviewNotice) gone — the gate only ever existed to avoid Codex-calling-Codex recursion, which dissolved once review went cross-vendor; whole-branch adversary review now runs under a Codex host too (a silent-skip bug, now fixed). **FOREGROUND PATH PRESERVED:** `codexSuppressed`/`--codex-suppressed`/`dispatch_foreground` in `lib/continue.mjs`/`lib/dispatch/ops.mjs`/`shouldSuppressWorkflow` is the no-Workflow-tool execution path (Codex/Pi host) and is untouched — only the *rescue* and *review-gating* uses were removed. Back-compat: legacy `codex_host_suppressed` skip reasons in old bundles' `events.jsonl` still surface via `lastSkip` (read-path intact). **Prompt/docs:** `commands/masterplan.md` §0 rekeyed `suppressRescue`→`isCodex`, rescue dispatch instruction dropped, `[--codex-suppressed]` dropped from `mp finish-step` (kept on `mp continue`); `AGENTS.md`/`README.md`/`docs/install.md` Codex-host prose rewritten to the foreground path. **Track B doc reconciliation (in-scope incompleteness):** `docs/contracts/masterplan-contracts.md` — fixed the sole heading↔`contract_id` mismatch (`adversary.review_serial_v1`→`step-c.adversary_review_serial_v1`) the rename introduced, and rewrote the EXEC (`codex_exec_v1`) + serial-REVIEW algorithm bodies off `codex:codex-rescue` (EXEC: `target:'codex'` is log-only, real backend is `resolveImplementerBackend`→mp-implementer/inline; REVIEW: agent-dispatch adversary lane, dropping the now-moot same-vendor asymmetric rule since the lane is cross-vendor to both Claude and Codex); `docs/conventions/adversarial-review-failure-policy.md` (new Track B file) — removed its "Rescue path (codex:codex-rescue)" section. **Verified:** opus fresh-eyes code-reviewer confirmed no dangling `suppressRescue`, foreground path intact, legacy read-path intact; cross-vendor gpt-5.5 review lane was DOWN (epyc2 LiteLLM gateway `/health` timeout) so the same-vendor reviewer was the fallback. Suite **937/936**; the 1 fail is the pre-existing `agents/mp-implementer.md` tools-regex (`mcp__skynet__*` underscores, skynet-rebrand commit 198de3c) — unrelated. **DEFERRED follow-up:** `docs/conventions/api-retry-policy.md` (committed-unchanged) still names `codex:codex-rescue` as a live dispatch path — a focused doc-purge, tracked here; historical `docs/masterplan/*` retros/specs left as-is (records of past runs). Excluded from this commit (user-owned, separate workstreams): `lib/dispatch/adsp-adapter.mjs`+test (adsp-v1), `docs/masterplan/*/state.yml`+`.bak` (path-rebrands), `.owner.hb.*` (volatile run locks).

## 2026-06-18 — `codex.review` defaults to on at seed + finish-time defensive gate (hindsight-historian fix)

Bundle `codex-review-issues` (4 tasks, 4 waves; planning via `paseo run` to codex/gpt-5.5 because the local skynet/minimax-m3 dispatchers returned `chain_exhausted` and the claude/opus path hit `MiniMax API 401`). User's post-mortem of the hindsight-historian run identified a single primary cause (`codex.review` was never armed) plus three compounding factors that made the gap invisible: (a) execution path rerouted to same-vendor Claude general-purpose agents + Claude "adversarial verify" (looked like review, was Claude-on-Claude); (b) finish-time safety net defeated three ways — `detectBase` returned null (no main/master), `--codex-suppressed` was set by host-misclass, flag wasn't armed; (c) the proposed-but-not-implemented `codex_review_configured_but_zero_invocations` doctor check can't catch the never-configured case. **Fix:** **`buildSeedState` now defaults `codexReview: true`** (the only behavior change; everything else is durable visibility for what was already there); explicit opt-out via `mp seed --codex-review=off` (A9 absent-field: explicit off omits the field). **`lib/finish-step.mjs` gate restructured** into 7a/7b/7c/7d: 7a computes `effectiveArmed = codexArmed || defensiveArmed` (legacy bundle missing `state.codex` entirely → defensively armed once, presence-scoped, with `codex_review_defensively_armed` event for audit); 7b expands base via `detectBaseAuto` (local → origin → any remote → `EMPTY_TREE_SHA` last resort); 7c emits `codex_review_skipped` with a typed reason (sha-keyed, idempotent at HEAD); 7d runs review. **The `branch_finish` AUQ payload now carries a `notice` field** surfacing the skip reason / suppression / defensive-arm note so the user sees WHY review didn't run, not just that it didn't. **`lib/finish.mjs`:** new `detectBaseAuto(branchesText, remoteBranchesText)` (pure, unit-testable from fixture text) + `EMPTY_TREE_SHA` export. **Tests:** 14 new (7 in `test/finish.test.mjs` for detectBaseAuto + EMPTY_TREE_SHA; 7 in `test/finish-step.test.mjs` for defensive arm, skip events, AUQ notice, re-entry guard, explicitCodex fixture flag) — `makeFixture` defaults `codex: { review: false }` so existing tests keep their semantics (the old implicit-no-codex behavior, which would now defensively-arm everything). Suite **951/952**; the 1 pre-existing failure is `agents/mp-implementer.md` tools-regex (underscore in `mcp__skynet__*` tool names) — unrelated, predates this work. **No migration of existing bundles** — the defensive arm is a one-time per-bundle event at the finish gate; users re-seed if they want the new default-on semantics in `state.yml`. Wave 0 (`mp seed` default + CLI flag) / Wave 1 (finish-step defensive gate) / Wave 2 (extended tests) / Wave 3 (docs across README, `docs/verbs.md`, `commands/masterplan.md` §3, `skills/masterplan/SKILL.md`, CHANGELOG, WORKLOG). Out of scope: new `codex_review_configured_but_zero_invocations` doctor module (user picked the no-audit option). Bundle `docs/masterplan/codex-review-issues/` is the durable record; committed on `masterplan/codex-review-issues` branch via 4 wave commits (Wave 1 squashed with a fixup commit for the `freshEvents` re-read that D6 first reverted; same final hash via `git rebase --autosquash`).

## 2026-06-10 — dispatch decision logic consolidated into `lib/dispatch/` (prep for unified cross-tool agent dispatch)

User ask: pull all agent-dispatching logic into a separate module ahead of a unified dispatch system spanning more than masterplan. Survey found the logic in FOUR places, so this was a consolidation, not an extraction: `lib/routing.mjs` (already pure), `lib/codex-host.mjs`, a private qctl-gate block in `lib/wave.mjs`, and the dispatch-vehicle fork inline in `lib/continue.mjs`. **New package `lib/dispatch/`** (import via `index.mjs`): `routing.mjs` + `host.mjs` are git-mv'd verbatim; `backend.mjs` lifts `qctlEligible`/`INFRA_RE`/allowlist matching + the previously-private `resolveBackend` (now public `resolveTaskBackend`, fail-closed `{kind:'agent'}` downgrade unchanged); `ops.mjs` lifts `normalizeReviewMode` + `buildWaveDispatchOp` (the Residual-3B `dispatch_foreground` vs `launch_workflow` fork — op shapes are a wire contract with the shell's §2 op table, now pinned by test). **Boundary rule (index header): decision logic IN, state machine OUT** — everything in the package is pure (no fs/clock/subprocess/git) so it can lift verbatim into a shared package later; `prepareWave`/`verifyScope`, the continue trampoline, the L2 engine (can't import — consumes pre-resolved routing via `args`), the qctl job contract, and `codex-companion.mjs` (finish-gate review, not dispatch) stay consumers and are listed in the header with reasons. `wave.mjs` re-exports `qctlEligible` for historical importers. Behavior byte-identical; routing/codex-host tests now import through the facade; +7 `test/dispatch.test.mjs` (facade surface, gate composition, op-shape deepEqual). Suite **903/903**, doctor exit 0. Docs repointed (README L2 box, wave-dispatch/plan-parser/task-verification/internals, planner agents). The /srv/dev/agent-dispatch repo stays the POLICY side; this package is masterplan's MECHANISM side. lib/ change only → reaches live sessions at the next marketplace deploy (user-gated, no release cut here — CHANGELOG entry parked under [Unreleased]).

## 2026-06-10 — v9.1.1: doctor stops fighting the writer; dev-repo doctor 15 WARN → 0; deployed to all 4 accounts

"Fix everything" sweep over the dev repo's 15 doctor WARNs surfaced two REAL check bugs, both shipped as v9.1.1 (`f1bd53e`, fixes in `e7971b9`): **(1) scalar-cap warned on the writer's own canonical output** — `lib/bundle.mjs` serializes `state.tasks` as one inline-JSON line, so any run with a few tasks tripped the 200-char cap; worse, the WARN's fix text told operators to move it to an overflow file, which would corrupt resume — the very hand-edit the `--fix` handler already refused (`typeof parsedState[key] !== 'string'`). The check now carries the same structured-value exemption (positively-parsed non-strings only; unparseable state stays conservative). **(2) legacy-bundle's code contradicted its docstring** — the contract says README-only `docs/superpowers/` must not warn; `hasLegacyArtifacts` flagged any `.md`. Now exempts README.md. Rest of the WARNs were genuine cruft, pruned per the clean verb's designed path (all archived, git history retains): 8 legacy schema<6 bundles (+ their gitignored telemetry sidecars), `docs/superpowers/` (two pointer READMEs whose v7-era purpose — suppressing a deleted skill's grep — was discharged), `docs/design-residuals.md` (all 4 residuals CLOSED; outcomes already in WORKLOG/code; the §3 Codex parity-run obligation stays tracked here, not lost). The duplicate-worktree-claim WARN died with its three claimants (all pruned bundles). `docs/masterplan/README.md` stale superpowers sentence rewritten. Tests: scalar-cap structured test flipped WARN→PASS (asserting the now-coherent check+fix contract), new fixtures `pass-structured-tasks` + `pass-readme-only-superpowers`. Suite **893/893**, dev-repo doctor **0 ERROR / 0 WARN**. Deployed same-session to all 4 accounts (grojas@epyc1 + codex mirror, ras@epyc1/epyc2/skynet3) via the rsync-overlay + `claude plugin update` pipeline — all "9.1.0 → 9.1.1", cache bins smoke-verified. Still UNPUSHED (user-gated); ras hosts keep `autoUpdate:false` until the push.

## 2026-06-10 — finish-time docs-normalization offer (`docs_normalize` gate) + `plan-doc-cruft` doctor check (#14)

User ask: at finish, offer to fold plan-organized docs into the repo's category-organized documentation and strip plan provenance; old cruft should eventually surface and go away. AUQ rulings: run-touched docs at finish + repo-wide via doctor; archived bundles untouched (`clean` prunes); offer only when candidates exist, config-suppressible. **Machine (`lib/finish-step.mjs` step 4.5):** after the dirty-commit, BEFORE verification — the normalization commit moves WT HEAD before `verified_sha` is recorded, so verification + the codex review run once over the FINAL tree. Candidates = three-dot `--diff-filter=ACMR -- '*.md'` vs detected base, bundle dir excluded (`filterDocCandidates`, pure, in `lib/finish.mjs`); base undetectable → silent skip. Guard is **presence-keyed** durable events (`docs_normalize`/`docs_normalize_skipped`) NOT sha-keyed — normalization moves HEAD by design, once per run. Two-phase like `push_pr`: the gate row instructs the LLM to fold/strip/commit in WT then re-invoke `--docs-normalized --docs-count=<n>` (or `--docs-skipped --docs-reason=..`); nothing durable changes until the flag, death mid-edit re-renders. Zero candidates → fully silent, no event. `state.docs.normalize: off` suppresses durably, `--docs-suppressed` per-invocation. Gate payload stays `{id, opened_at}` (scalar-cap discipline; candidates recomputed on re-render). **Doctor:** new `lib/doctor/plan-doc-cruft.mjs` (auto-discovered, #14) — anchored to ARCHIVED bundles, warns on md outside the runs dir with slug-token filenames / `docs/masterplan/<slug>` body refs / hyphenated-slug headings; excludes runs dir, docs/superpowers (legacy-bundle owns it), ALL dot-dirs (first live run FP'd on `.claude/plans` + `.worktrees` before that exclusion), node_modules, root WORKLOG/CHANGELOG*/HISTORY, >1 MiB; always WARN never ERROR. Live run: exactly 1 genuine WARN (docs/design-residuals.md → 2026-05-29-v8-dogfood). **Tests:** finish-step 18→28 (offer/re-render, normalize/skip events + gate clear, zero-candidate silence, bundle-only-md silence, config + per-invocation suppression, no-base skip, answer-replay idempotence, dirty-md ordering, full flow to archive; learned: porcelain w/o `-uall` collapses untracked new-dir files to `?? dir/` — test seeds tracked-modified md), doctor +9 incl. dispatcher count 13→14 (append-only around the user's dirty parseArgs/runFixes/scalar-cap-fix hunks), prompt-structure +3 entries. Fixtures `test/fixtures/doctor/plan-doc-cruft/` (6 scenarios). Prompt: §2c row + intro, §2 gates list, §2d stop-set, finish verb row; BONUS: removed a resurrected `dispatch_wave` mention at §2d (pre-existing prompt-structure failure on HEAD). Suite **891/891**, doctor exit 0. Deploy to live plugin caches = marketplace pipeline, user-gated as usual.

**LATER SAME SESSION — v9.1.0 released internally + deployed to all 4 accounts.** Release commit `7d1c53d` on local main: the feature above + the user's in-flight doctor `--fix` autofix work (runFixes/`--fix` CLI/scalar-cap fix handler — committed together as one coherent surface, suite green with it) + version 9.0.0→9.1.0 across the four sync'd manifests, README, CHANGELOG. **NOT pushed to GitHub** (user-gated; origin/main still 9.0.0). Deployed out-of-band via `git archive` export → rsync overlay into each account's `marketplaces/rasatpetabit-masterplan` clone → `claude plugin update`/`install` (reads the LOCAL clone): grojas@epyc1 update 9.0.0→9.1.0 (+ `~/.codex/.tmp/marketplaces` mirror), ras@epyc2 + ras@skynet3 update 9.0.0→9.1.0, ras@epyc1 first canonical install (marketplace add → overlay → install; orphaned legacy `masterplan@masterplan-v8` uninstalled). All 4 verified: installed_plugins.json shows 9.1.0 + cache bin smoke `v9.1.0`. **New hazard learned:** `claude plugin update` AUTO-REFRESHES a github-source marketplace clone (`autoUpdate:true`) from origin BEFORE comparing versions — a clean clone resets to origin/main, wiping the rsync overlay ("already at the latest version"). Fix: flip `autoUpdate:false` in `~/.claude/plugins/known_marketplaces.json` first (done on ras@epyc1/epyc2/skynet3; left false to protect the unpushed deploy). grojas' clone was immune (sits on local commits diverged from origin) and keeps `autoUpdate:true`. After the eventual v9.1.0 push: restore autoUpdate true + reset clones to origin. Live sessions still need user-only `/reload-plugins` per host.

## 2026-06-10 — v9.0.0 released internally; CHANGELOG/WORKLOG pre-v8 history deep-compacted

`feat/v9-record-result` ff-merged to local main (`ccd307d`, user ruled "merge locally only") with a final Codex r7 PASS (zero findings; it independently re-ran the targeted tests 29/29 and verified sweep never deletes branches + gates win before active-run recovery). This commit: **pre-v8 history deep-compacted** — CHANGELOG 1087→64 lines (v1.0.0…v7.2.3 → one bullet per major series under "Historical — pre-v8"), WORKLOG 560→128 (2026-05-22…05-30 → "Pre-v8 history" block; Codex's tail-appended review entry relocated above it). Full text recoverable via each file's git history and tag `v8.1.0-pre-cruft-removal`. **Version → 9.0.0** in all four sync'd manifests + README + CHANGELOG entry. **LATER SAME SESSION: pushed + tagged after all** — user upgraded the ruling at the close-out AUQ: origin/main 62b25ba→92d3700, tag `v9.0.0`, release-publish workflow auto-published the GitHub release (2026-06-10T06:32Z), both CI runs green; the marketplace autoUpdate regression risk below is therefore CLOSED. The deploy itself had already gone out-of-band before the push: this committed tree was rsync-mirrored into each host's `marketplaces/rasatpetabit-masterplan` clone, then `claude plugin install`/`update` (which reads the LOCAL clone, no GitHub fetch) produced **genuine 9.0.0 installs** on all three hosts — epyc1 updated 8.2.0→9.0.0, epyc2 normalized off the orphaned `masterplan@masterplan-v8` entry (canonical install + legacy uninstall), skynet3 first-ever install. All 7 deployed copies checksum-verified against the export; bin smoke-ran on each host. Caveats: gitCommitSha labels lag (cosmetic); marketplace clones are github-source with `autoUpdate:true`, so any marketplace update from GitHub (still at 8.2.0/62b25ba) would REGRESS the clone until v9 is pushed; live sessions need a restart/`/reload-plugins` (user-only) to load 9.0.0. Hazard learned: never `rsync --delete` into a cache version dir without `--exclude=.in_use` (per-PID session markers live there). Empirical gates unchanged: T2.3 toy-task dogfood, T2.4 full-finish dogfood, T2.5 Codex-hosted parity run.

## 2026-06-10 — Codex r6 fixes: merge-replay strand + suppressed-host planning (branch feat/v9-record-result)

Codex r6 (cross-vendor branch review of 2b332ec vs ca22070) returned 2 P2s, both genuine, fixed on-branch per the standing directive. **(P2) full-teardown crash replay stranded the run:** death after `worktree remove` + successful post-merge `branch -d` but BEFORE the disposition write meant the replayed `--choice=merge` re-ran `git merge` on the now-deleted ref → permanent `dispatch-error` with the gate open. Fix in `lib/finish-step.mjs`: the merge block is skipped when WT-gone AND branch-ref-gone — that pair is proof the prior merge landed (the post-merge `-d` is the sole branch deleter), so the replay falls through to removal-by-absence retirement (`removed_after_merge`). **(P2) SKILL.md overclaimed "never returns launch_workflow" while Codex-hosted planning could still emit it:** `recover_plan_run` and fresh plan-phase resume both routed a suppressed host to the parallel plan fan-out (Workflow-tool-only). Fix in `lib/continue.mjs`: under `codexSuppressed`, `recover_plan_run` clears the stale plan marker (drafters are read-only — fully re-derivable) and reroutes to `resume-phase` with `planning_mode:'serial'`; `resume_phase` forces `'serial'` for phase==='plan'. §3a step 1 + the SKILL.md Workflow row now state the serial-forcing accurately. **Tests:** finish-step 18→19 (full-teardown replay retires, no dispatch-error), continue 9→10 (serial forced on resume; plan-marker recovery reroutes + clears `active_run`; unsuppressed paths unchanged). Suite **864/864**, doctor exit 0. Empirical gates unchanged (T2.3/T2.4 dogfoods, T2.5 Codex parity run). Next: merge `feat/v9-record-result` → main (user-gated).
