# WORKLOG

## 2026-06-10 — dispatch decision logic consolidated into `lib/dispatch/` (prep for unified cross-tool agent dispatch)

User ask: pull all agent-dispatching logic into a separate module ahead of a unified dispatch system spanning more than masterplan. Survey found the logic in FOUR places, so this was a consolidation, not an extraction: `lib/routing.mjs` (already pure), `lib/codex-host.mjs`, a private qctl-gate block in `lib/wave.mjs`, and the dispatch-vehicle fork inline in `lib/continue.mjs`. **New package `lib/dispatch/`** (import via `index.mjs`): `routing.mjs` + `host.mjs` are git-mv'd verbatim; `backend.mjs` lifts `qctlEligible`/`INFRA_RE`/allowlist matching + the previously-private `resolveBackend` (now public `resolveTaskBackend`, fail-closed `{kind:'agent'}` downgrade unchanged); `ops.mjs` lifts `normalizeReviewMode` + `buildWaveDispatchOp` (the Residual-3B `dispatch_foreground` vs `launch_workflow` fork — op shapes are a wire contract with the shell's §2 op table, now pinned by test). **Boundary rule (index header): decision logic IN, state machine OUT** — everything in the package is pure (no fs/clock/subprocess/git) so it can lift verbatim into a shared package later; `prepareWave`/`verifyScope`, the continue trampoline, the L2 engine (can't import — consumes pre-resolved routing via `args`), the qctl job contract, and `codex-companion.mjs` (finish-gate review, not dispatch) stay consumers and are listed in the header with reasons. `wave.mjs` re-exports `qctlEligible` for historical importers. Behavior byte-identical; routing/codex-host tests now import through the facade; +7 `test/dispatch.test.mjs` (facade surface, gate composition, op-shape deepEqual). Suite **903/903**, doctor exit 0. Docs repointed (README L2 box, wave-dispatch/plan-parser/task-verification/internals, planner agents). The /srv/dev/agent-dispatch repo stays the POLICY side; this package is masterplan's MECHANISM side. lib/ change only → reaches live sessions at the next marketplace deploy (user-gated, no release cut here — CHANGELOG entry parked under [Unreleased]).

## 2026-06-10 — v9.1.1: doctor stops fighting the writer; dev-repo doctor 15 WARN → 0; deployed to all 4 accounts

"Fix everything" sweep over the dev repo's 15 doctor WARNs surfaced two REAL check bugs, both shipped as v9.1.1 (`f1bd53e`, fixes in `e7971b9`): **(1) scalar-cap warned on the writer's own canonical output** — `lib/bundle.mjs` serializes `state.tasks` as one inline-JSON line, so any run with a few tasks tripped the 200-char cap; worse, the WARN's fix text told operators to move it to an overflow file, which would corrupt resume — the very hand-edit the `--fix` handler already refused (`typeof parsedState[key] !== 'string'`). The check now carries the same structured-value exemption (positively-parsed non-strings only; unparseable state stays conservative). **(2) legacy-bundle's code contradicted its docstring** — the contract says README-only `docs/superpowers/` must not warn; `hasLegacyArtifacts` flagged any `.md`. Now exempts README.md. Rest of the WARNs were genuine cruft, pruned per the clean verb's designed path (all archived, git history retains): 8 legacy schema<6 bundles (+ their gitignored telemetry sidecars), `docs/superpowers/` (two pointer READMEs whose v7-era purpose — suppressing a deleted skill's grep — was discharged), `docs/design-residuals.md` (all 4 residuals CLOSED; outcomes already in WORKLOG/code; the §3 Codex parity-run obligation stays tracked here, not lost). The duplicate-worktree-claim WARN died with its three claimants (all pruned bundles). `docs/masterplan/README.md` stale superpowers sentence rewritten. Tests: scalar-cap structured test flipped WARN→PASS (asserting the now-coherent check+fix contract), new fixtures `pass-structured-tasks` + `pass-readme-only-superpowers`. Suite **893/893**, dev-repo doctor **0 ERROR / 0 WARN**. Deployed same-session to all 4 accounts (grojas@epyc1 + codex mirror, ras@epyc1/epyc2/skynet3) via the rsync-overlay + `claude plugin update` pipeline — all "9.1.0 → 9.1.1", cache bins smoke-verified. Still UNPUSHED (user-gated); ras hosts keep `autoUpdate:false` until the push.

## 2026-06-10 — finish-time docs-normalization offer (`docs_normalize` gate) + `plan-doc-cruft` doctor check (#14)

User ask: at finish, offer to fold plan-organized docs into the repo's category-organized documentation and strip plan provenance; old cruft should eventually surface and go away. AUQ rulings: run-touched docs at finish + repo-wide via doctor; archived bundles untouched (`clean` prunes); offer only when candidates exist, config-suppressible. **Machine (`lib/finish-step.mjs` step 4.5):** after the dirty-commit, BEFORE verification — the normalization commit moves WT HEAD before `verified_sha` is recorded, so verification + the codex review run once over the FINAL tree. Candidates = three-dot `--diff-filter=ACMR -- '*.md'` vs detected base, bundle dir excluded (`filterDocCandidates`, pure, in `lib/finish.mjs`); base undetectable → silent skip. Guard is **presence-keyed** durable events (`docs_normalize`/`docs_normalize_skipped`) NOT sha-keyed — normalization moves HEAD by design, once per run. Two-phase like `push_pr`: the gate row instructs the LLM to fold/strip/commit in WT then re-invoke `--docs-normalized --docs-count=<n>` (or `--docs-skipped --docs-reason=..`); nothing durable changes until the flag, death mid-edit re-renders. Zero candidates → fully silent, no event. `state.docs.normalize: off` suppresses durably, `--docs-suppressed` per-invocation. Gate payload stays `{id, opened_at}` (scalar-cap discipline; candidates recomputed on re-render). **Doctor:** new `lib/doctor/plan-doc-cruft.mjs` (auto-discovered, #14) — anchored to ARCHIVED bundles, warns on md outside the runs dir with slug-token filenames / `docs/masterplan/<slug>` body refs / hyphenated-slug headings; excludes runs dir, docs/superpowers (legacy-bundle owns it), ALL dot-dirs (first live run FP'd on `.claude/plans` + `.worktrees` before that exclusion), node_modules, root WORKLOG/CHANGELOG*/HISTORY, >1 MiB; always WARN never ERROR. Live run: exactly 1 genuine WARN (docs/design-residuals.md → 2026-05-29-v8-dogfood). **Tests:** finish-step 18→28 (offer/re-render, normalize/skip events + gate clear, zero-candidate silence, bundle-only-md silence, config + per-invocation suppression, no-base skip, answer-replay idempotence, dirty-md ordering, full flow to archive; learned: porcelain w/o `-uall` collapses untracked new-dir files to `?? dir/` — test seeds tracked-modified md), doctor +9 incl. dispatcher count 13→14 (append-only around the user's dirty parseArgs/runFixes/scalar-cap-fix hunks), prompt-structure +3 entries. Fixtures `test/fixtures/doctor/plan-doc-cruft/` (6 scenarios). Prompt: §2c row + intro, §2 gates list, §2d stop-set, finish verb row; BONUS: removed a resurrected `dispatch_wave` mention at §2d (pre-existing prompt-structure failure on HEAD). Suite **891/891**, doctor exit 0. Deploy to live plugin caches = marketplace pipeline, user-gated as usual.

**LATER SAME SESSION — v9.1.0 released internally + deployed to all 4 accounts.** Release commit `7d1c53d` on local main: the feature above + the user's in-flight doctor `--fix` autofix work (runFixes/`--fix` CLI/scalar-cap fix handler — committed together as one coherent surface, suite green with it) + version 9.0.0→9.1.0 across the four sync'd manifests, README, CHANGELOG. **NOT pushed to GitHub** (user-gated; origin/main still 9.0.0). Deployed out-of-band via `git archive` export → rsync overlay into each account's `marketplaces/rasatpetabit-masterplan` clone → `claude plugin update`/`install` (reads the LOCAL clone): grojas@epyc1 update 9.0.0→9.1.0 (+ `~/.codex/.tmp/marketplaces` mirror), ras@epyc2 + ras@skynet3 update 9.0.0→9.1.0, ras@epyc1 first canonical install (marketplace add → overlay → install; orphaned legacy `masterplan@masterplan-v8` uninstalled). All 4 verified: installed_plugins.json shows 9.1.0 + cache bin smoke `v9.1.0`. **New hazard learned:** `claude plugin update` AUTO-REFRESHES a github-source marketplace clone (`autoUpdate:true`) from origin BEFORE comparing versions — a clean clone resets to origin/main, wiping the rsync overlay ("already at the latest version"). Fix: flip `autoUpdate:false` in `~/.claude/plugins/known_marketplaces.json` first (done on ras@epyc1/epyc2/skynet3; left false to protect the unpushed deploy). grojas' clone was immune (sits on local commits diverged from origin) and keeps `autoUpdate:true`. After the eventual v9.1.0 push: restore autoUpdate true + reset clones to origin. Live sessions still need user-only `/reload-plugins` per host.

## 2026-06-10 — v9.0.0 released internally; CHANGELOG/WORKLOG pre-v8 history deep-compacted

`feat/v9-record-result` ff-merged to local main (`ccd307d`, user ruled "merge locally only") with a final Codex r7 PASS (zero findings; it independently re-ran the targeted tests 29/29 and verified sweep never deletes branches + gates win before active-run recovery). This commit: **pre-v8 history deep-compacted** — CHANGELOG 1087→64 lines (v1.0.0…v7.2.3 → one bullet per major series under "Historical — pre-v8"), WORKLOG 560→128 (2026-05-22…05-30 → "Pre-v8 history" block; Codex's tail-appended review entry relocated above it). Full text recoverable via each file's git history and tag `v8.1.0-pre-cruft-removal`. **Version → 9.0.0** in all four sync'd manifests + README + CHANGELOG entry. **LATER SAME SESSION: pushed + tagged after all** — user upgraded the ruling at the close-out AUQ: origin/main 62b25ba→92d3700, tag `v9.0.0`, release-publish workflow auto-published the GitHub release (2026-06-10T06:32Z), both CI runs green; the marketplace autoUpdate regression risk below is therefore CLOSED. The deploy itself had already gone out-of-band before the push: this committed tree was rsync-mirrored into each host's `marketplaces/rasatpetabit-masterplan` clone, then `claude plugin install`/`update` (which reads the LOCAL clone, no GitHub fetch) produced **genuine 9.0.0 installs** on all three hosts — epyc1 updated 8.2.0→9.0.0, epyc2 normalized off the orphaned `masterplan@masterplan-v8` entry (canonical install + legacy uninstall), skynet3 first-ever install. All 7 deployed copies checksum-verified against the export; bin smoke-ran on each host. Caveats: gitCommitSha labels lag (cosmetic); marketplace clones are github-source with `autoUpdate:true`, so any marketplace update from GitHub (still at 8.2.0/62b25ba) would REGRESS the clone until v9 is pushed; live sessions need a restart/`/reload-plugins` (user-only) to load 9.0.0. Hazard learned: never `rsync --delete` into a cache version dir without `--exclude=.in_use` (per-PID session markers live there). Empirical gates unchanged: T2.3 toy-task dogfood, T2.4 full-finish dogfood, T2.5 Codex-hosted parity run.

## 2026-06-10 — Codex r6 fixes: merge-replay strand + suppressed-host planning (branch feat/v9-record-result)

Codex r6 (cross-vendor branch review of 2b332ec vs ca22070) returned 2 P2s, both genuine, fixed on-branch per the standing directive. **(P2) full-teardown crash replay stranded the run:** death after `worktree remove` + successful post-merge `branch -d` but BEFORE the disposition write meant the replayed `--choice=merge` re-ran `git merge` on the now-deleted ref → permanent `dispatch-error` with the gate open. Fix in `lib/finish-step.mjs`: the merge block is skipped when WT-gone AND branch-ref-gone — that pair is proof the prior merge landed (the post-merge `-d` is the sole branch deleter), so the replay falls through to removal-by-absence retirement (`removed_after_merge`). **(P2) SKILL.md overclaimed "never returns launch_workflow" while Codex-hosted planning could still emit it:** `recover_plan_run` and fresh plan-phase resume both routed a suppressed host to the parallel plan fan-out (Workflow-tool-only). Fix in `lib/continue.mjs`: under `codexSuppressed`, `recover_plan_run` clears the stale plan marker (drafters are read-only — fully re-derivable) and reroutes to `resume-phase` with `planning_mode:'serial'`; `resume_phase` forces `'serial'` for phase==='plan'. §3a step 1 + the SKILL.md Workflow row now state the serial-forcing accurately. **Tests:** finish-step 18→19 (full-teardown replay retires, no dispatch-error), continue 9→10 (serial forced on resume; plan-marker recovery reroutes + clears `active_run`; unsuppressed paths unchanged). Suite **864/864**, doctor exit 0. Empirical gates unchanged (T2.3/T2.4 dogfoods, T2.5 Codex parity run). Next: merge `feat/v9-record-result` → main (user-gated).

## 2026-06-10 — T2.5: Codex full-lifecycle hosting (Residual 3B) + Codex r5 finish-step fixes (branch feat/v9-record-result)

Increment 5 of the prose→code program, folded together with the Codex review of T2.4 (r5: 1 P1 + 2 P2, all genuine, fixed on-branch per the standing directive — the verdict gates merge/push, never the next increment). **T2.5 — `dispatch_foreground`:** under host suppression (`codexHostSuppressed`), `dispatchWave` in `lib/continue.mjs` returns `{op:'dispatch_foreground', wave, cwd, tasks, baseline, review, next:'record-result'}` instead of `launch_workflow` — same frozen scope/baseline/launching marker (no task_id → no probe; a crash re-emits the same op via recover_and_redispatch). The host runs the routed tasks sequentially (Codex inline + `update_plan`; CC fallback one awaited `mp-implementer` per task) and feeds the standard digest array to `mp record-result` — identical lifecycle from there. Taught in the §2 op table + SKILL.md's Workflow row (rewritten from "not yet landed" to the delivered op); `docs/attic/v7-codex-hedge/` `git rm`'d (manifest Tier-4 #13 FULLY discharged, addendum added; design-residuals Residual 3 closing addendum; `codex-host.mjs:5` comment repointed at tag `v8.1.0-pre-cruft-removal`). **HONESTY GATE OPEN: unit-verified only — no Codex-hosted parity run yet; do not claim dogfooded Codex execution.** **Codex r5 fixes on finish-step:** (P1) `--choice=pr` archived (kept_by_user + clear-gate) BEFORE the shell pushed — death pre-push silently archived with no PR; now a two-phase handshake: bare `pr` returns `shell push_pr` with NOTHING durable changed (gate stays open, re-renders on death), `--choice=pr --pushed` retires+archives. (P2) merge ran against whatever MAIN had checked out — now guarded: MAIN's HEAD branch must equal the detected base or → `dispatch-error` (never auto-checkout the user's MAIN). (P2) death between `worktree remove` and the disposition write wedged re-entry (gate re-render died on WT git) — now the branch_finish re-render hydrates MAIN-side (`refs/heads/<branch>`, `wt_missing:true`) and a replayed choice treats the absent path as removal-confirmed (`worktree prune` + retire). **Tests:** finish-step 16→18 (pr handshake incl. crash-before-push; merge target guard; teardown crash window), continue +1 (foreground op shape, markers, crash re-emit, two-wave lifecycle to finish), prompt-structure extended (`dispatch_foreground` op, `--pushed` flag). Suite **862/862**, doctor exit 0. Outstanding empirical gates: T2.3 toy-task dogfood, T2.4 full-finish dogfood, T2.5 Codex parity run (deploy user-gated). Next: merge `feat/v9-record-result` → main (user-gated).

## 2026-06-10 — T2.4: `mp finish-step` — the §2c finish state machine moves to code (branch feat/v9-record-result)

Increment 4 of the prose→code program. The §2c finalization flow (165 lines of prose transaction) and the §2e¶7 teardown are now ONE re-entrant trampoline: the shell calls `mp finish-step`, executes the ONE op returned (`run_verify | write_retro | run_codex_review | ask | shell | stop`), and re-invokes with the answer threaded back as flags (`--verify-passed/-failed`, `--codex-done/-skipped` + count/base/digest-file/reason, `--choice=merge|pr|keep|discard`, `--removal-force`, `--retro-only`). **New `lib/finish-step.mjs` (`finishStep`)** internalizes every durable transaction: Guard D acquire/heartbeat → retired-disposition re-entry shortcut (MAIN-side, NO WT git — the Codex P1) → WT snapshot + task-scope dirty-commit (unrelated dirt untouched; HEAD refresh re-keys the verified check) → verified-at-SHA skip → retro write-if-absent gating → sha-keyed codex re-entry guard (`codex_review`/`codex_review_skipped` events; the skip event now carries `data.sha` so a re-walk can't loop the review) → durable `branch_finish` gate open + the `--choice` resolution transaction (merge `--no-edit` with abort-on-conflict → `dispatch-error`, gate intact; `worktree remove` with disposition from the ACTUAL outcome via `dispositionAfterTeardown`; best-effort branch retire `-d`/`-D`; event + clear-gate + bundle commit) → archive LAST + release-owner. Shell keeps only LLM/network work: running verification (skill), writing the retro, the codex-companion network run, the PR probe, the push (`shell push_pr` op), the AUQs. The `no_verification_command` gate stays shell-opened (`mp open-gate`) but resolves through the same `--verify-passed/-failed` flags (both verification gates are in finishStep's clear set). **Bug found while testing:** `runGit` trims output — porcelain's first line ` M path` lost its leading status column and `classifyDirt` mis-sliced the path; finish-step reads porcelain raw (`gitPorcelain`). **Prompt 626 → 501 lines**: §2c replaced by the op-table contract, §2e¶7 by the internalized-teardown summary, §2 rows + §2e¶2/¶4/¶6/¶8 repointed; `mp finish-status`/`record-verification`/`codex-review-status`/`set-worktree-disposition` no longer appear in the prompt (guarded by prompt-structure ABSORBED). **Tests:** `test/finish-step.test.mjs` 16 on real temp git MAIN+worktree+bundle+owner fixtures — happy path to archive, re-entry at EVERY boundary, verify-fail gate + pass override, dirty-commit + stale-verified-SHA, codex done/skip durable guards + suppression, merge/pr/keep/discard, merge-conflict abort, choice replay guard (no double merge/event), crash-leaked WT loud error, retro-only terminal, Guard D block + lock-off, split-commit discipline (no `.owner*` in bundle commits). Also: `record-result` bin now honors `owner_lock=off` without a session id (Codex r1 P2, regression-tested, d2488e0). Suite **859/859** (836 + 16 + 7 structural), doctor exit 0. Pending: dogfood one full finish + Codex branch review of the T2.4 commits; next: T2.5 Codex full-lifecycle hosting.

## 2026-06-10 — T2.3: `mp continue` — the §2 trampoline; the prompt stops being the resume engine (branch feat/v9-record-result)

Increment 3 of the prose→code program. The §2 resume controller is now a TRAMPOLINE: locate bundle → session-gated `mp sweep --apply` → catch-completion-first → loop on `mp continue` executing ONE typed op per call (`launch_workflow | probe | run_skill | ask | stop`, 13-row table). **New `lib/continue.mjs` (`continueRun`)** internalizes what was §2/§2a-launch/§2b-launch prose: Guard D acquire+confirm (the per-entry re-acquire IS the open-turn heartbeat; `blocked`/`lost` → `ask` ops, NEVER auto-force), migrate-on-load (`<state>.v<N>.bak`), wave backfill from `plan.index.json`, worktree create-or-reuse (`ensureWorktree` — the probe/add/record transaction, closing the crash-between-add-and-record window in code), crash-recovery scope reset, phase-1 `active_run` marker (frozen F-SCOPE scope + D6 baseline), `routeTask` routing, and the inline `finalize_run` reconcile (same `mp record-result --reconcile` transaction). Flags thread the shell's answers back in: `--alive/--dead`, `--stale-reconciled`, `--force`, `--codex-suppressed`. **New `lib/sweep.mjs` (`mp sweep`)** — the §2e¶5 orphan sweep with the SAFETY INVERSION ruled in the plan: dry-run is the bare default (report-only), `--apply` executes repair/prune/`worktree remove --force`/`rm -rf`+prune/durable normalize; `manual` (foreign-unverified etc.) is never automated in either mode. The v9 seam holds: `mp` runs only the LOCAL `-C`-qualified git its transactions need; dispatch, AUQs, and all network git stay shell-side; `mp continue` writes state but never commits (CD-7 split unchanged — record-result commits both sides). **Prompt 704 → 626 lines**: §2 replaced by the ~60-line loop contract + op table, §2a reduced to the RESULT half, §2b-launch to 3 steps, §2e¶4/¶5 to pointers; ~25 stale references rewritten (`dispatch_wave`/`surface_gate`/`prepare-wave`/`step 1.6` etc. → ops). **Tests:** `test/continue.test.mjs` 8 (op-sequence against seeded temp repos: fresh-launch, owner-blocked, probe/reap, legacy-refused, reconcile, gate, finish, plan-phase) + `test/sweep.test.mjs` 3 (dry-run touches nothing, apply executes + skips manual, foreign-leftover + idempotence) + `test/prompt-structure.test.mjs` 10 (the prompt no longer contains absorbed sequences; ops + deliberate survivors present). Suite **835/835** (814 + 21), doctor exit 0. Pending: full dogfood run on a toy task + Codex branch review; next: T2.4 `mp finish-step`, T2.5 Codex full-lifecycle.

## 2026-06-10 — T2.2: `mp record-result` — the wave-completion transaction moves to code (branch feat/v9-record-result)

Increment 2 of the Fable-era prose→code program (Thrust 2 of the approved plan; design rule: the LLM stops being the transaction engine, `mp` gains LOCAL git). The §2a Completion steps 0–4, the §2 `finalize_run` reconcile prose, and the §2e¶6 split-commit trace are now ONE crash-safe subcommand. **New `lib/wave-commit.mjs` (`recordWaveResult`):** owner heartbeat (lost-to-other → return with ZERO writes) → mark digests in-memory + one atomic `writeState` (the LEADING durable action; `markTask` throws on unknown id before any byte hits disk) → D6 `after` capture + `verifyScope` off the frozen `active_run.{scope,baseline}` → out-of-scope revert split by trackedness (`checkout` for tracked, `clean -fd` for the rest — plain `checkout --` ERRORS on untracked paths) → code commit in WT, pathspec-scoped, done-files-only when the wave has failures (so recover's `checkout -- resetPaths` actually resets partial edits) → marker clear iff the whole wave is done → `wave_recorded` event + state commit in MAIN → `decideNextAction` returned as advisory `next` (never thrown over). Reconcile mode (`--reconcile`, result:null) IS the finalize_run row — a clean WT degrades every git step to a no-op, marker clear + state commit still land. **Latent prose bug the tests caught:** the prose's `git add docs/masterplan/<slug>` would sweep Guard D's `.owner.lock`/`.owner.hb.*` sentinels into the state commit (shipping a stale lock to every clone); the code excludes them by pathspec `:(exclude)<bundle>/.owner*`. **bin:** new `record-result` case (`--result-file` preferred over inline `--result` — no shell-quoting hazards; lost-to-other exits 0, it's a valid outcome the shell AUQs on) + the owner trio refactored onto a shared `resolveOwnerSelf` (behavior unchanged). **Prompt 726 → 704 lines** — §2a completion is now: write result JSON to a scratch file OUTSIDE the bundle dir (a bundle-dir scratch would be swept into the state commit) → one `mp record-result` → act on the returned JSON; §2e¶6 + ¶8 + the §2/CD-7 rows repointed. **Tests:** `test/wave-commit.test.mjs` 8/8 on real temp git repos with linked worktrees (clean wave / out-of-scope revert / dirty-WT crash reconcile + double-reconcile refusal / split-commit isolation incl. foreign staged content on both loci / lost-to-other zero-writes / failed-task partial-stage + recover handoff / preconditions / qctl-stays-pending). Suite **814/814** (baseline 806 + 8), doctor exit 0. Pending: one manual dogfood wave + Codex branch review (`--scope branch --base main`); next increments: `mp continue` trampoline (3), `mp finish-step` (4), Codex full-lifecycle (5).

## 2026-06-10 — v8.2.0: v7 cruft cutover + CI realignment EXECUTED (branch feat/v8.2.0-cruft-cutover; push user-gated)

Executed the long-deferred cutover-removal-manifest (2026-05-29-v8-dogfood bundle) in 6 phases, each suite-green (806/806, doctor 0). **Why now:** a Fable-5-era ground-up reevaluation (qwen + 3 Explore agents + GPT-5 Codex adversarial review + 2 Plan agents) found main had NO real CI — ng-ci.yml was bound to the deleted masterplan-ng branch and ci.yml ran the v7 release-gate battery the manifest deletes. **Phase 0 fixed CI first** (node --test + doctor + symlink assert; release-publish job kept), then tag `v8.1.0-pre-cruft-removal` → Tier-1/2 deletion (~17.5k lines: 16 bin/*.sh, telemetry hook, lib python, parts/, tests/ 302 files) → reference scrub + contracts-registry relocation to docs/contracts/ (DF-1 RESOLVED, DF-3 MOOT) → doc realignment (~251→~800 line claims, 11→13 doctor modules + coord-drift/owner-sentinel rows, parseReservedVerbs, publish/follow in SKILL.md, CD-10) → disposition-enum dedupe + qctl helper-table wiring → version 8.2.0 + CHANGELOG (8.x headers bracket-normalized for the release-publish awk). **Key rulings** (user AUQ 2026-06-10): masterplan-detect KEPT (Tier-3 reversal wins over the Tier-1 row); Tier-4 #13 discharged via docs/attic/v7-codex-hedge/ + SKILL.md Workflow row (3B still committed, lands as Thrust-2 increment 5 of the approved improvement program at ~/.claude/plans/bubbly-doodling-sparkle.md — next: prose quarantine, then mp record-result/continue/finish-step). Manifest errata addendum records both. Note: the 2026-06-10 Codex review entry (now relocated above the pre-v8 compacted block) was appended BY Codex during its read-only review (sandbox write-through); kept, rode into commit 0c538d7. Untracked legacy/ + __pycache__ removed. **SHIPPED same session** (user ruled merge-now): ff-merged to main (179faac), pushed, tagged; the new release-publish job auto-published the v8.2.0 GitHub release and the rewritten ci is GREEN on main (first time the real suite has ever run there; the pre-cruft tag predictably fails the OLD v7 gate at its historical commit — benign, and the v7 gate was already red on prior main pushes). T2.1 prose quarantine then executed on feat/v9-prose-quarantine (819→726 lines, −11.3KB: §6.5 → docs/design/qctl-multi-repo-apply.md, publish/follow rows → docs/coordination-playbook.md, both bannered). Marketplace deploy (/plugin marketplace update + /reload-plugins) = still user-only. Next increment: T2.2 mp record-result.

## 2026-06-06 — Branch/PR/worktree sweep EXECUTED + notes migrated (user-gated cleanup done)

Acted on the gated cleanup from the entry below (user approved via AUQ). **PR #14** (`feat/configurable-runs-dir-discovery`) **closed as superseded** — re-verified its only feature (`MASTERPLAN_RUNS_DIR` env-aware discovery) already ships in main at `lib/paths.mjs:resolveRunsDir`, and `git diff --stat main..pr14` is a ~13.3k-line net *deletion* (merging reverts v8). **All stale branches deleted** (SHAs captured for recovery): local `feat/b3-push-waiter@3e15865`, `pr14@df7febb`, `masterplan-ng@d793e34`; remote `codex-routing-fix@2b03ca3`, `fix/codex-degraded-false-positive@8e1e6b6`, `masterplan-ng@d793e34`, `masterplan/cc3-visibility@55d2510`, `v5.0.0-lazy-phase-prompts@7d74d4d`, `v6@2338c63`. Each verified pre-v8/net-deletion vs main before deletion. **origin now holds only `main@25f17cd`**; local holds only `main`. **Worktree `.worktrees/masterplan-ng` pruned** — its 12 gitignored canonical memory notes (the MEMORY.md link targets, absent from main's tree) were **migrated into `docs/notes/`** (still gitignored, .gitignore:60) first; MEMORY.md links rewritten `.worktrees/masterplan-ng/docs/notes/ → docs/notes/` + header corrected (the "NFS-shared" claim was false — `/home` & `/srv/dev` are local ZFS). **Deploy:** grojas@epyc1 (this host) already v8.1.0 ✓; per user decision **ras@epyc1/ras@epyc2 skipped** (ras@epyc1 stays 8.0.0 — no passwordless sudo; ras@epyc2 unreachable, DNS). Current-session live activation still needs user-only `/reload-plugins`. **Untouched:** `mp-dogfood/` (local nested repo) + `rasatpetabit/mp-dogfood` (GitHub) — §7 dogfood unconfirmed, cleanup not yet due. main is committed + tagged `v8.1.0` + pushed; remote tag verified present (`refs/tags/v8.1.0 → 25f17cd`).

## 2026-06-06 — Release v8.1.0: worktree lifecycle + Guard D shipped; branch/PR landscape resolved

The worktree-lifecycle + dispatch-disjointness + Guard D hardening (the entry below) **shipped**: committed `7e604a5` on `main`, ff-merged, pushed to GitHub. Manifests bumped `8.0.0 → 8.1.0`, CHANGELOG v8.1.0 added, tagged `v8.1.0`. Branch/PR sweep: every "ahead" branch verified **already-in-main or superseded by the v8 rebuild** (none carried live unmerged work) — `qctl-implementer-backend`/PR #14 would *revert* main (main is a strict content superset); `fix/codex-degraded-false-positive`/`codex-routing-fix`/`chore-handbook` are pre-v8 surfaces; `feat/b3-push-waiter` clean-merges but only edits dead `parts/step-b.md` (inert in the v8 sequencer). Remote-branch deletes, PR #14 close, and the b3 port were gated to the user. **Deploy:** grojas marketplace clone ff'd; `/plugin marketplace update` + `/reload-plugins` is user-only; `ras@epyc2` unreachable from this host (no DNS/hosts/ssh-config entry, `/srv/dev` is ZFS not NFS).

## 2026-06-06 — Guard D: NFS-safe cross-session owner sentinel (Phase 3b) — provisional-acquire, live-perfect / stale-benign — SHIPPED in v8.1.0 (7e604a5, pushed)

Built the never-shipped Guard D from the worktree/concurrency hardening plan (task #6): mutual exclusion for two LLM sessions operating the **same run bundle** concurrently (epyc1/epyc2, NFS). `state.yml` can't provide it (`writeState` is an atomic write, not test-and-set), so this is a dedicated NFS-safe lock.

**Identity = SESSION, not process.** Every `mp` call is an ephemeral OS process (pid/start-time churn each call; `kill(pid,0)` always dead), so the stable anchor is `{host, CLAUDE_CODE_SESSION_ID}` (a UUID → no PID-reuse guard needed). Liveness is heartbeat-age TTL only (default 30m; must exceed the max single background wave, since the LLM only heartbeats at turn/wave boundaries). **Two files (double-ownership guard):** immutable `.owner.lock` (atomic `link()`-create, confirmed via `stat().nlink===2` — NOT link()'s return, which some NFS clients mis-report after a transparent retry) + per-owner `.owner.hb.<host>.<session>` the owner refreshes for ITSELF only (never rewriting the lock → a stalled owner's late heartbeat can't clobber a successor).

**The honest guarantee (NOT "perfect single-writer" — that's impossible on NFS without a lock manager):** Guard D gives **PERFECT mutual exclusion for LIVE contention** — a fresh contended lock is an atomic `link()` create, so two live sessions never both proceed (the loser cleanly resolves to `blocked`). The **unit of protection is the TURN**, not the individual write: the shell re-heartbeats at every turn entry (step 1.6) and before the completion write (§2a step 0), and under live contention exactly one contender resolves to `held-by-self` while the rest get `lost-to-other` and STOP. The **one residual, accepted by design:** a `>TTL`-abandoned owner (silent 30+ min) that RESURRECTS at the exact instant a reclaimer breaks its lock can double-write. Narrow, benign, documented — not a gap to close with another mechanism.

**The hard part — the stale-break storm, and why acquire is PROVISIONAL (design, dead-ends, resolution):** a stale lock must be removed before the empty path can be re-`link()`'d, but there is **no atomic "remove path P iff it still resolves to inode I" on NFS** (or POSIX). So the removal is unavoidably path-based (`renameSync(lockPath, .dead.*)`), and under a concurrent break storm a late breaker can evict a FRESH successor and let a 2nd acquirer slip onto the now-empty path — i.e. **two sessions can transiently report `acquire`/`steal`**. Reproduced: N=60 yielded 2 reported winners ~1-in-5 (the on-disk lock stayed single, so the safety property already held — but the *reported* outcome over-promised). **Dead-ends explored + rejected (4 iterations):** (1) a generation-named break-token won by exclusive `link()` — Codex (gpt-5.5, xhigh) found 3 BLOCKERs (release+reacquire still evicts a fresh successor; `forceBreak` unserialized; crash-leaked token wedges the generation). (2) a mutation-mutex M — its leak self-heal needs a small TTL that breaks cross-host clock-skew robustness. advisor() confirmed each is a subtler version of the same NFS impossibility. **Resolution — stop trying to make acquire exactly-once; make it honestly PROVISIONAL.** Dropped the break-token; `stealBreak` is a plain generation+TTL-gated rename-away whose generation check only **reduces churn**, never claims safety. The heartbeat re-check (`heartbeatOwner` → `held-by-self` for *exactly* the on-disk owner) is the boundary that **resolves transient multi-winner churn into a single proceeding writer** — the shell runs it after a provisional win (step 1.6 confirm branch) and before every state write (§2a step 0). **Evidence:** stress invariant relaxed to the correct one — acquire-winners ≥1 (churn allowed), but **`held-by-self==1` + `lost-to-other==N-1`** at the heartbeat boundary; N=60 yields `hbs==1`.

**Codex round-2 review (provisional-acquire focus, brief `/tmp/codex-guard-d-review2.txt`) — 2 BLOCKER + 1 MAJOR, resolved:** (a) **BLOCKER-1 (real): `heartbeatOwner` read→write TOCTOU** — a steal landing between the initial read/decide and the state write let a displaced owner write off a stale `held-by-self`. **Fix 1 (applied):** `heartbeatOwner` now WRITE-CONFIRMS — refresh our hb first (makes a concurrent stale-break re-confirm see us fresh and bail), then RE-READ `.owner.lock` and claim `held-by-self` only if it's STILL ours; else unlink the hb we just wrote and return `lost-to-other`. advisor() confirmed this shrinks the window to the same benign stale-takeover residual. (b) **BLOCKER-2 (claim-defect, not a machinery gap): "the shell doesn't heartbeat before every write"** — advisor ruled the fix is to document the **turn-scoped** guarantee, NOT to build a `writeState` ownership chokepoint (iteration-5 trap: an env-reading check on the universal write path to chase a benign residual). Verified turn-entry coverage: step 1.6 fires on every §2 entry, §2a step 0 on completion. (c) **MAJOR: `releaseOwner` unlink race** — read-at-top then unlink-later could clobber a successor that stole+recreated the lock in the window. **Fix 3 (applied):** re-read immediately before the unlink; only remove a lock still `isSelf`. **Both fixes proved by deterministic fs-wrapper unit tests** (inject a steal between the read and the post-write/pre-unlink re-read; the `nlink===2` test is the template). The "authoritative single-writer gate" language was the over-claim Codex's BLOCKER-2 keyed on — **reframed everywhere** to live-perfect / stale-benign (owner.mjs + owner-fs.mjs headers, masterplan.md step 1.6 + §2e¶8, owner-stress.sh, this entry).

**Codex round-3 review (the two surgical Fix-1/Fix-3 fixes, brief `/tmp/codex-guard-d-review3.txt`) — 1 BLOCKER (real), resolved; Fix-1 CLEARED:** Codex cleared Fix-1 (`heartbeatOwner` write-confirm closes the read→write TOCTOU; residual is strictly the documented stale-takeover case). But it found Fix-3's `releaseOwner` **re-read-then-path-unlink still races**: the re-read only protects the window BEFORE it — the path-`unlink(.owner.lock)` after `isSelf(before)` still fires, so a successor B that steals our STALE lock and recreates it in the window AFTER our re-read but BEFORE our unlink gets its FRESH lock clobbered → strands a live successor + opens a B+C double-write. **Root-cause (the consistency insight, advisor-confirmed):** `acquire`, `heartbeat`, and `stealBreak` all consult the staleness predicate — **only `releaseOwner` blindly path-unlinked**, the one lock-op that bypassed it. **Fix (freshness gate, applied):** `releaseOwner` now path-unlinks ONLY a lock proven still WITHIN TTL — a successor can only steal a STALE lock, so fresh ⟹ no successor can exist to clobber; a stale owned lock returns the new `stale-not-released` outcome and is **left for the reclaim path** (a stale lock is exactly what a reclaimer is entitled to break). ttlEff mirrors `acquireOwner` (ttlMs when threaded, else `OWNER_TTL_DEFAULT_MS`) so release + acquire share ONE staleness predicate; the gate engages whenever `now` is known (bin defaults `--now` to `Date.now()` → engaged in every production release). **advisor's implementation catch:** the freshness sample reads our own hb via `readLastHeartbeat`, but the old code deleted that hb EARLY → a post-delete sample falls back to `acquiredAt` → false "stale" → A refuses to release its OWN fresh lock → strands the bundle. **Resolution:** sample freshness BEFORE deleting the hb; keep the hb on disk through the critical window (load-bearing — a concurrent breaker re-reads it and bails when we're fresh); on the release path unlink the LOCK first, our hb LAST. With all four lock-ops now gated on staleness there is **no remaining op that path-mutates a lock it hasn't proven fresh-and-self** — the stop criterion (no 5th op to hole).

**Files (all NEW/untracked unless noted):** `lib/owner.mjs` (pure `decideOwnership` + identity/serialize/paths + the guarantee/limit header), `lib/owner-fs.mjs` (NFS-safe link/rename/stat/unlink layer; provisional `stealBreak`; Fix-1 write-confirm `heartbeatOwner`; **freshness-gate `releaseOwner`** — `stale-not-released` + ttlEff-mirrors-acquire), `lib/doctor/owner-sentinel.mjs` (WARN on corrupt/stale lock + orphan hb), `bin/masterplan.mjs` (+`acquire-owner|heartbeat-owner|release-owner` fs-only subcommands; release now threads `now`+`ttlMs`), `commands/masterplan.md` (step 1.6 acquire→provisional-win→**heartbeat-confirm-before-dispatch**→blocked-AUQ; §2a step-0 heartbeat re-check→lost-to-other STOP; §2c step-6 release; §2e¶8 doc + guarantee/limit bullet), `test/owner-stress.sh` (real-fs acceptance gate; Scenario 2 asserts the heartbeat-boundary invariant). Verified: **owner units 38/38** (replaced the now-impossible Fix-3 steal-injection test with a stale-refuse + fresh-pass pair for the freshness gate), full **suite 791 pass / 0 fail**, **doctor exit 0** (0 err / 26 warn — the cc3-orphan + duplicate-claim warns are the Phase-2 doctor work correctly surfacing the live broken state, gated for Phase 4; rest is pre-existing stale-codex-task noise), **stress N=60 ALL SCENARIOS PASS** (`hbs==1`/`lost==59`).

**Deferred-with-reason:** the literal cross-host NFS leg of acceptance can't run here — `/srv/dev/masterplan` is ZFS-local on epyc1 (no NFS mounts) and epyc2 is unreachable (no DNS/hosts entry). Acceptance rests on the same-host high-concurrency ZFS stress (exercises the real `link()`/`stat(nlink)`/`rename` atoms) + the `nlink===2` mis-report unit test + the two deterministic TOCTOU tests. Cross-host leg is the one remaining real-NFS verification if/when both hosts are reachable.

**Next / gated (task #7, user-gated — do NOT run without explicit go):** Phase 0 `git reset HEAD` on main (un-stage the 606 accidental deletions; cause unexplained, could recur); Phase 4 cc3-visibility quarantine→diff→`rm -rf`+`git worktree prune`; redeploy LAST (carries the fixed tree). **Nothing committed** (main's index is wiped — a commit would record the staged deletions).

## 2026-06-05 — Per-run worktree lifecycle: shell wiring + round-3 cross-vendor fold (5 findings) — NOT committed (main index wiped)

Wired the bundle-in-MAIN / code-in-WT per-run worktree lifecycle into the L1 sequencer (`commands/masterplan.md` §2/§2a/§2c + the new §2e locus model) on top of the Phase-1 plumbing (`lib/worktree.mjs` + `mp worktree plan|record|reconcile`, tasks #1–#5/#8). **Design (kept):** the run **bundle** (`docs/masterplan/<slug>/`) always lives in MAIN on `base`, addressed absolute-MAIN everywhere; **WT** = `<MAIN>/.worktrees/<slug>` on `masterplan/<slug>` holds **code only** → the branch never commits the bundle dir → `merge masterplan/<slug>→base` at finish is conflict-free. cwd is a **write-only** signal to about-to-launch agents (`cd "<WT>"` before each wave → implementers inherit cwd=WT); every shell git is `-C`-qualified by locus (the sole bare git is the MAIN-derivation bootstrap). Split commit: code→`git -C "<WT>"` (scoped add), state→`git -C "<MAIN>"`. Global orphan sweep runs once/session at first §2 entry; teardown layers onto `finishing-a-development-branch` via `mp worktree record --choice`.

A background cross-vendor Codex pass (gpt-5.x, read-only) on the wiring returned **REVISE → 5 findings, all folded** this round:
- **[P1] §2c snapshot-after-teardown:** the WT HEAD snapshot ran before the disposition shortcut, but §2e¶7 teardown removes `<WT>` before re-entering `complete` → the snapshot died on a missing worktree. **Fix:** a "re-entry shortcut precedes the snapshot" clause reads `worktree_disposition` MAIN-side (no WT git); retired → jump straight to archive, never snapshot WT.
- **[P1] split-commit crash not fully recoverable:** a death after `mp mark-task` but before verify-scope+code-commit left `finalize_run` to only clear the marker + commit MAIN state → verify-scope and the code commit silently skipped. **Fix:** `set-active-run --baseline` persists the D6 `before` into the marker (`promote` carries it; `verify-scope` falls back to it when `--before` omitted), and the `finalize_run` row now reconciles a dirty WT (re-run verify-scope off the persisted baseline + revert out-of-scope + code-commit) before clearing. Crash-trace prose (§2a step 3, §2e¶6) corrected.
- **[P1] create-or-reuse not idempotent** across a crash between `git worktree add` and `mp worktree record` (live registered WT, no `state.worktree` → next `plan` re-`create`s → `add` fails on the present dir). **Fix:** `planWorktreeCreate({registered})` reuses on the canonical path being registered; `mp worktree plan --worktree-registered`; §2e¶4 + §2a step-0 probe `git -C "<MAIN>" worktree list`.
- **[P2] `<MAIN>` used in the discovery glob before it was derived** → MAIN now derived first (step 1), WT/branch + sweep after the slug is known (step 1.5).
- **[P2] §2a step-0 `worktree record` missing the required `--state`** → added + pointed at §2e¶4 as canonical.

Code: `lib/worktree.mjs` (`registered`), `bin/masterplan.mjs` (`worktree plan --worktree-registered`; `set-active-run --baseline`; `promote` carry-forward; `verify-scope` baseline fallback), `commands/masterplan.md` (§2 MAIN-first, §2a step-0/step-3, §2c re-entry-shortcut-before-snapshot, §2e¶4/¶6, `finalize_run` row). Tests: +5 (2 lib `planWorktreeCreate` registered, 3 bin: baseline persist/carry/fallback, `--worktree-registered` reuse). Verified: **suite 748 pass / 0 fail**, **doctor exit 0**. The round-3 Codex re-review **wedged on its own sandbox `node --test` stall** (a known tooling hang for this repo — not a finding); its interim static pass explicitly validated the baseline fix ("coherent: `verify-scope` uses `active_run.baseline` only when `--before` is absent; `promote` carries both `scope` and `baseline`"). Closure rests on the local suite + doctor + the task-#8 `lib/worktree.mjs` Codex pass (**PASS, no findings**). **Nothing pushed; nothing committed** (main's index is wiped — a commit would record the staged deletions; gated on user).

Cross-vendor Codex review of the P2 fix (`d10c627`, below) returned three findings; this closes all three. **P2 #2 (the important one):** the channel split fixed `JSON.parse(--data)` but NOT bash quoting — the findings digest was still interpolated into `mp event --note="<digest>"`, so a stray `"`/backtick/`$()`/newline in Codex-rendered output could break the shell word (dropping the durable event → re-opening the very P2) or inject a later `--data=`. Fix: new **`--note-file=PATH`** transport on `mp event` — the shell `Write`s the digest to `<bundle>/codex-review-digest.txt` (the Write tool is not shell-evaluated) and passes the PATH; `bin` reads the bytes verbatim into `record.note`; mutually exclusive with `--note`. §2c step-5 prose switched to Write-then-`--note-file`. The git-derived `--summary`/`--data` scalars (`<base>`, `{sha,base,count}`) stay interpolated — trusted refnames/hashes. **P3:** `codex-review-status` collapsed *every* `events.jsonl` read error to `{present:false}`; now only `ENOENT` does, any other (`EACCES`/`EISDIR`/IO) dies loud (a swallowed error would falsely re-run the network gate or look "skipped"). **P2 #1:** §2c prose now states the residual window honestly (death between reviewer-exit-0 and event-land re-runs the review on resume — harmless/idempotent at unchanged HEAD; `open-gate` never double-opens). `surface_gate` row also rehydrates `base` now.

Code: `bin/masterplan.mjs` (`--note-file` handler + doc-header + ENOENT-only catch in `codex-review-status`); `commands/masterplan.md` (§2c step-5 Write-then-`--note-file` + residual-window clause; §2 surface_gate `base`). Tests: +4 integration (`test/bin-masterplan.test.mjs`) — adversarial-bytes verbatim round-trip, `--note`/`--note-file` mutual exclusion, unreadable `--note-file`, non-ENOENT/EISDIR fail-loud. Verified: suite **656 pass** / 2 pre-existing unrelated `stale-lock` fixture ENOENTs; doctor exit 0; smoke 45/45; hook OK; CC-3 grep PASS. Cross-vendor Codex re-review of `019127e`: **all 3 CLOSED, no new defects.** **Nothing pushed.**

## 2026-06-03 — P2 durability/re-entrancy fix for the whole-branch codex-review finish-gate — branch `feat/codex-finish-gate`

Closes the Codex [P2] finding against the §2c step-5 success path: the prior text claimed "this body runs once; on resume `surface_gate` re-renders the AUQ but does not re-enter step 5, so the review never re-runs" — true only once the gate is *open*. The uncovered window is a death **between the review completing and `mp open-gate`**: no `pending_gate` yet ⇒ resume re-decides to `complete` ⇒ §2c re-runs from the top ⇒ the expensive network-bound review fires **again**. Second half: the rendered findings digest lived only in the live turn's context, so a compaction-then-`surface_gate` re-render **lost the digest**.

Fix (prose-only on the shell, leaning on a new fs-only read subcommand + pure helper — no new git/io in the shell):
- **Durable re-entry guard.** Step 5 now writes the success record with the **channel split** (advisor-locked): `--summary` = audit signal (unchanged phrasing, still matches `\bcodex\s+review\b`), `--data '{sha,base,count}'` = quote-safe machine scalars the guard keys on, `--note` = the free-text digest. The digest is kept OUT of `--data` on purpose — a stray quote/backtick/newline in interpolated Codex output would break the subcommand's `JSON.parse(--data)` and silently drop the record, re-introducing the P2. The event lands **before** `open-gate`, so on (re-)entry step 5 first calls `mp codex-review-status --sha=$(git rev-parse HEAD)`; `{present:true}` ⇒ skip the re-run + rehydrate + emit no event.
- **Digest rehydration.** §2 `surface_gate` row now re-reads `codex-review-status` when re-rendering `branch_finish` and folds `digest`/`count` back into the AUQ — the durable `--note` survives compaction, the live one doesn't.

Code: `lib/codex-companion.mjs` pure `selectCodexReviewForHead(eventsText, sha)` (scans events.jsonl text, last SUCCESS `codex_review` at the sha wins; `codex_review_skipped` ignored; `present` keys on existence not count>0); `bin/masterplan.mjs` `codex-review-status` fs-only subcommand (+ import + doc-header); `commands/masterplan.md` §2c step-5 guard + 3-channel event-write + §2 surface_gate rehydration. Tests: +8 unit (`test/codex-companion.test.mjs`) + 1 integration (`test/bin-masterplan.test.mjs`, end-to-end `mp event`→`mp codex-review-status`).

Verified: suite 652 pass (the same 2 pre-existing doctor `stale-lock` fixture-utime ENOENTs, unrelated); doctor exit 0; policy smoke 45/45; hook syntax OK; CC-3-trampoline grep PASS. **Nothing pushed.**

## 2026-06-03 — whole-branch codex-companion review gate at finish + self-contained CC-3-trampoline — branch `feat/codex-finish-gate`

Two asks. **(a) Net-new whole-branch codex review finish-gate.** Add a `codex-companion review --scope branch --base <base>` pass as a lead-in to §2c step 5 (the `branch_finish` gate) — the one place review-mode's whole-branch unit is correct, a capability v8 lacked. **codex-companion (not `mp-codex-reviewer`) is the given mechanism.** Decisions: foreground/blocking with an OUTER `timeout 600` ceiling — review-mode is the network-dependent native Codex *cloud* reviewer, its `--wait`/`--background` flags are no-ops (it always runs foreground) and its internal status-wait is 240 s, so the shell bounds it above that so a hang can't wedge finish; **fail-soft** — any non-success (non-zero / `124` / unresolved path / `codex_host_suppressed` / config off) → degraded-skip event + PROCEED, never block. Gate condition = `codex_review` ∧ `base` ∧ NOT `codex_host_suppressed` ∧ resolved companion path. **Audit-channel design lock:** SUCCESS summary `"codex review complete (whole-branch, base <base>) — <n> findings"` matches the audit's `\bcodex\s+review\b` → suppresses `codex_review_configured_but_zero_invocations` (correct: it ran); SKIPPED summary `"whole-branch codex-companion review skipped (degraded) — …"` has a hyphen that breaks the regex → does NOT falsely suppress (correct: a degraded finish where nothing reviewed should still flag). Both verified against the live regexes. Version-agnostic path resolver (fs-only `mp codex-companion-path` + pure `lib/codex-companion.mjs` + unit test) derives the active codex install from `installed_plugins.json` (installPath encodes the live version).

Code: `bin/masterplan.mjs` (finish-status emits `codex_review` mirroring the `state.codex.review` dispatch predicate; `mp event --summary` — the audit-scanned signal channel vs free-text `--note`); `lib/codex-companion.mjs` + `test/codex-companion.test.mjs` (12 tests); `commands/masterplan.md` §2c step-5 lead-in + snapshot return-shape; smoke gating-proof fixture (real `codex_review --summary` event suppresses the zero-invocations warning; paired no-event control still fires). **(b) Self-contained CC-3-trampoline.** §2d "Turn-close routing" + the §5 close prose no longer defer to dormant `parts/step-0.md` — the sequence is now defined in-file; the `CC-3-trampoline` token is preserved so `check_cc3_trampoline`'s grep stays green (`parts/step-0.md` untouched per the explicit "no deep retire" scope).

Verified: suite 643/645 (the 2 fails are pre-existing doctor `stale-lock` fixture-utime ENOENTs, unrelated — the doctor's own `stale-lock` check PASSes); doctor exit 0; policy smoke 45/45; hook syntax OK; CC-3-trampoline grep PASS both arms. Fresh-eyes Explore caught + fixed a missing `codex_review` field in the §2c snapshot return-shape. **Nothing pushed** — branch awaits disposition; cross-vendor Codex review pending.

## 2026-06-02 — qctl implementer-backend seam (Part A, contract-first) — branch `qctl-implementer-backend`

Wire petabit-sysadmin's Qwen Work Fabric (`pi`/`qctl` worker) as a **pluggable implementer backend** so masterplan execute waves + github-coordination followers can offload task implementation to the GPU fleet instead of always dispatching `mp-implementer`. **Contract-first**: the fabric's `qctl` (its task 11) + `gate.py` (its task 12) don't exist yet, so the *live binding* is feature-flagged **off** and a qctl-kind task records a **NotYetBound blocked** digest — the seam, the descriptor union, and the §4 qctl results-contract are built now so binding is later a small swap, not a rewrite. Design: `docs/superpowers/specs/2026-06-01-qctl-implementer-backend-design.md`; plan: `docs/superpowers/plans/2026-06-01-qctl-implementer-backend.md`.

**Hard invariant — flag off ⇒ byte-for-byte identical to today.** Held by: `buildSeedState` never emits `implementer` (audited — 0 matches in `lib/bundle.mjs`); `resolveImplementerBackend` defaults to bare `{kind:'agent'}` (carries no agentType/model — the existing `execute.workflow.js` `implAgentType`/`implModel` dogfood seam stays sole owner); the `implement(t)` guard is `t.backend?.kind === 'qctl'` only, so the absent-backend legacy path and `{kind:'agent'}` are untouched.

Code (4 files, additions-only): `lib/routing.mjs` (`resolveImplementerBackend` beside `routeTask`); `lib/wave.mjs` (`prepareWave` stamps per-task `backend`); `bin/masterplan.mjs` (`prepare-wave` threads `state.implementer ?? {}`); `workflows/execute.workflow.js` (NotYetBound guard — *blocked* digest, never a throw: a throw nulls the pipeline item → silent vanish → re-dispatch loop). Docs: `commands/masterplan.md` follow-row + `docs/config-schema.md` flag block.

Verified: suite **510/0** (scoped `node --test test/` — bare `node --test` ELOOPs on the pre-existing recursive `.worktrees/…/superpowers-masterplan` symlink). Codex defect pass (`review --scope branch --base main`): default agent path confirmed intact; **2 P2s, 0 P0/P1**, both follower-path docs-only, both fixed in `6caaa16` — (1) a github-coordination follower carries no lead `state.yml`, so backend must resolve **lead-side at publish time** and ride the issue/contract, not follower-local state (threading deferred to binding time); (2) NotYetBound now → `mp:blocked` not `mp:open` (re-adding open re-queued the unserviceable task into a claim/comment/release loop). **Nothing pushed** — branch awaits disposition.

## 2026-06-01 — Codex review de-bloat: disable gstack `review` skill globally + harden mp-codex-reviewer

Symptom: every Codex defect/diff review (ad-hoc `codex exec` / `/codex:review` on any repo) ballooned to ~3240 lines because the model **auto-elected the third-party gstack `review` skill** and `cat`'d its **1628-line `SKILL.md`** into the trace, burying the verdict. **Root cause is NOT masterplan:** `mp-codex-reviewer` already returns a digest (the dump never reached the orchestrator) — the visible bloat came from **direct cross-repo reviews** (openxcvr-kmod / xcvr-tools / /tmp). gstack/review is **model-elected** (trace: "Using the `review` skill…"), not hook-injected, and Codex has **no skills-off flag** (`codex features list` confirms — only `skill_mcp_dependency_install`).

**Fix, two surfaces.** (1) **Global, reversible (NOT in repo, edited in `~/.codex/skills/`):** renamed `SKILL.md`→`SKILL.md.mp-disabled` in BOTH gstack review copies (`gstack/review/` + `gstack/.agents/skills/gstack-review/`) so codex can't enumerate the skill — a *dir* rename would NOT work (codex keys identity off SKILL.md presence + frontmatter `name:`). Restore script: `~/.codex/skills/.mp-review-disabled-RESTORE.sh`. `gstack/review/SKILL.md` had uncommitted user edits — **preserved** in the `.mp-disabled` copy. A `gstack upgrade` may restore it → re-run the disable. (2) **In-repo:** `agents/mp-codex-reviewer.md` invocation now tells Codex "do NOT activate/read/echo any skill" (belt-and-suspenders + saves Codex wall-time even though this agent digests).

Verified: a fresh codex rollout's injected catalog has **0** review-skill entries (**71 other gstack skills intact** — surgical); suite **503/0**; doctor exit 0.

## 2026-05-31 — PR-awareness in report verbs + finish gate; autonomy auto-progress contract (`db1b84d`)

Two asks: (1) surface an open GitHub PR for the run's branch in the "what do I do next" routines; (2) **stop the over-asking under `--autonomy=loose`/`full`**. The load-bearing finding for (2): the asking was **not** masterplan's own gates — `decideNextAction` (`lib/resume.mjs`) already stops only at real gates, `routeTask`'s `target:'ask'` is dead code, and `autonomy` is stored-but-never-read by `decide`. The real driver was the **global `~/.claude/hooks/auq-guard.sh` Stop hook** blanket-blocking every substantive turn lacking an AUQ, plus the global `~/.claude/CLAUDE.md` "AUQ mandatory" prose that *contradicted* the loose-autonomy contract in the same file. **Decision: fix the prompt contract + the hook, NOT `mp decide`** — autonomy stays out of `decide` (it isn't the thing asking; advisor-steered, accepted). PR-awareness is **report-only** (never auto-merge) and kept **out of the per-turn `decideNextAction` loop** (a per-resume "merge your PR" would be a *new* nag) — it lives only in human-invoked `status`/`next`/`clean` + the `branch_finish` gate.

Repo (`db1b84d`, masterplan-ng only — never main): `lib/finish.mjs:summarizePr` + `mp pr-summary` (pure, gh-output-in, never throws on absent/malformed gh) + `commands/masterplan.md` §3 weave-in / §2c gate relabel + §2d autonomy stop-set & `<mp-autoprogress>` emission + §4 turn-close; +7 unit tests. **Global config (NOT in repo, edited in `~/.claude/`, live immediately):** `auq-guard.sh` — Modes A (ends `?`) & B (offloading phrases) still **block**; Mode C (flat-ending) downgraded **block→warn-only**; new `<mp-autoprogress>` escape hatch (assistant-side mirror of `<no-auq>`, emitted only on authorized loose/full non-gate turns). `CLAUDE.md` "End-of-turn handoff" realigned to "AUQ at genuine decision points", resolving the internal contradiction.

Verification: suite **358/358**; `bash -n` hook OK + **5/5** synthetic-transcript tests (marker→bail, flat→warn-only bail, Mode A→block, Mode B→block, AUQ-present→bail); `mp pr-summary` smoke green (empty / one-PR / gh-absent). Fresh-eyes Explore caught + fixed a false "complete stop-set" claim and a marker-action-list omission. **Standing (USER-gated):** `db1b84d` is NOT deployed (hosts at `5040f75`) and NOT pushed (origin `cf488b4`); the dogfood→push gate now finalizes `db1b84d` — see the gitignored `docs/notes/v8-dogfood-push-runbook.md` (refreshed this turn).

## 2026-06-10 — Adversarial architecture review of v8.1.0 control plane

Read-only design review of masterplan v8.1.0. Key conclusion: the live surface has grown beyond the documented thin-shell model; the highest-risk assumptions are prompt-as-control-plane, disproportional single-operator concurrency/distribution machinery, and stale/dormant v7/qctl/GitHub surfaces that should be pruned or moved behind explicit support gates. No source changes made. (Appended by Codex at the file tail during the review; relocated here at the 2026-06-10 compaction.)

## Pre-v8 history (deep-compacted 2026-06-10)

Entries from 2026-05-22 → 2026-05-30 (the v6/v7 era and the v8 clean-core rebuild) were deeply compacted on 2026-06-10; the full original text is in this file's git history (`git log -p -- WORKLOG.md`) and at tag `v8.1.0-pre-cruft-removal`.

- **2026-05-28 → 30 — v8 clean-core rebuild (masterplan-ng).** Steps 0–5 built the five-layer architecture from scratch — `lib/*.mjs` TDD core, thin sequencer shell, plugin-root agents, L2 Workflow engine, doctor modules — growing the suite 77 → 351. Repeated cross-vendor Codex adversarial reviews shaped it; dogfood Issues A–H fixed; design residuals ruled (2A worktree default-on; 3B Codex full-lifecycle deferred, later delivered 2026-06-10); the cutover-removal manifest was drafted (executed 2026-06-10 as v8.2.0). The epyc1/epyc2 history fork ended via merge `9c465ff` ("local as base"), deployed to both hosts out-of-band; v8.0.0 label bump + scoped epyc2 deploy via the registry-backed `masterplan-v8` marketplace; finish-flow Thrust A+B (durable `branch_finish` gate, archive-LAST, no silent archive) plus the re-entrancy review fixes (stale-`verified` re-snapshot, idempotent gate resolution, `no_verification_command` resume act); docs deep-clean.
- **2026-05-26 → 27 — v7 series.** Rename `superpowers-masterplan` → `masterplan` (7.0.x), per-verb skill stubs (7.1.x; removed in v8), ops-audit hardening (7.2.0), telemetry Check #53 (7.2.1), `/plan`-hijack fix (7.2.2), Codex dispatch guard + registry-drift detection (7.2.3). v6.3.3 published to the marketplace en route.
- **2026-05-22 → 23 — v6 series.** Token-efficiency v6.0.0 (step-c 110 KB → 4 lazy sub-files), adversarial review at B2/B3 + aggressive-loose (6.1.0), hoist-run-policy + Codex failure policy (6.2.x), improve-regression-detection + doctor tier fixes + post-merge drift scans (6.2.x–6.3.x), codex-hardening commits (background handle capture, output_path fallback, wave-barrier-interrupted detection, sandbox probe Check #48), Codex sandbox-worktree compat hotfix, improve-subagents-parallelism.
