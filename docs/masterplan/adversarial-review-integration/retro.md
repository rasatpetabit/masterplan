# Retro: adversarial-review-integration

**Slug:** adversarial-review-integration
**Completed:** 2026-05-22
**Complexity:** high
**Autonomy:** loose

---

## What we built

Integrated `codex:adversarial-review` automatically into masterplan's B2 (spec_approval) and B3 (plan_approval) gates. Specs and plans now get a challenge pass before the approval AUQ fires — structural problems are caught at the cheapest possible point (before code runs).

**Five files changed:**
- `parts/step-b.md` — B2 foreground dispatch block + B3 background dispatch block with resume logic
- `parts/step-0.md` — `--no-adversarial-review` CLI flag registered
- `docs/config-schema.md` — `adversarial_review: both` config field (off|spec|plan|both)
- `parts/doctor.md` — Check #44 (config validity) + Check #45 (gate-fire audit)
- `CHANGELOG.md` — v6.1.0 entry

---

## What went well

- **Spec was complete and unambiguous.** Every design decision (path discovery order, pass/fail heuristic, foreground vs background, companion-not-found fallback, aggressive-loose auto-close) was pre-decided in the spec. Execution was mechanical.
- **Parallel dispatch worked cleanly.** Tasks 1/2/5 ran in wave 1 (different files, no conflicts). Task 3 (B2 block) and Task 6 (CHANGELOG) ran in wave 2 in parallel. Task 4 (B3 block) followed Task 3 sequentially as required.
- **Companion-not-found fallback prevents workflow blocking.** The skip-on-missing logic ensures the review infrastructure absence never stops a run.

## Issues encountered

- **Doctor check numbers wrong in spec.** The spec specified new checks as #42 and #43, but both numbers were already taken (stale .lock file and codex_review_coverage). Caught during plan-writing by reading doctor.md — corrected to #44 and #45 in Task 1 before execution began.
- **Duplicate `spec:` key introduced in state.yml.** When appending the spec artifact pointer during the spec_gate transition, a duplicate YAML key was created. Caught and fixed at completion.
- **Spec listed wrong source file for CLI flag.** Spec said to add `--no-adversarial-review` to `commands/masterplan.md`, but the recognized flags table lives in `parts/step-0.md`. Plan corrected this without asking — the intent was clear.

## What to watch

- The B3 background review relies on `ScheduleWakeup` being available (loop mode). When not in loop mode, the AUQ fallback ("Poll now / Resume later") is the path. No production test of the background flow was possible in this session — manual smoke needed on first use.
- The pass/fail heuristic is intentionally liberal: only flags output matching `critical|fatal|serious|blocking|fundamental|wrong assumption`. Real adversarial reviews often note concerns without those keywords; this prevents false negatives from blocking the workflow.
- Doctor checks #44 and #45 will fire INFO/WARN against all pre-v6.1.0 bundles on next `/masterplan doctor` run. This is expected behavior per the check specs.

---

*Auto-generated by masterplan Step C completion retro (v6.1.0)*
