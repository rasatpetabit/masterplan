# Retrospective — codex-routing-fix

**Completed:** 2026-05-16  
**Duration:** ~45 minutes (16:00–16:45 UTC)  
**Complexity:** medium | **Autonomy:** loose  
**Waves:** 5 | **Tasks:** 15 | **Commits:** 10

---

## What shipped

| Wave | Tasks | Dispatch | Outcome |
|---|---|---|---|
| 1 | T1 (doctor #43), T2 (`subagent_return_bytes`), T3 (B2 aggressive Codex default) | parallel Codex EXEC × 3 | All 3 landed clean |
| 2 | T4 (eligibility_cache wave-pin), T5 (`dispatched_by` provenance), T6 (wave-mode 4b N-parallel review), T7 (asymmetric review enforcement) | serial Codex EXEC | All 4 landed; T6 ran inline-Claude per plan annotation (multi-file) |
| 3 | T8 (F6 audit + 5 contract registrations + audit-script hardening) | inline-Claude + Explore subagent | B4 audit findings written; 5 contracts registered; Pattern D verified via fixture |
| 4 | T9–T12 (4 failure classes) | Codex EXEC batched (1 dispatch → 4 sequential commits in c94b5cb) | Implementer-batching worked cleanly |
| 5 | T13 (internals.md), T14 (version bump + CHANGELOG), T15 (WORKLOG) | inline-Claude | v5.7.3 → v5.8.0 across all 4 manifests |

---

## What went well

- **Implementer-batching on Wave 4**: routing T9–T12 as one batched Codex EXEC to the same file (`parts/failure-classes.md`) instead of 4 serial dispatches saved 3 orchestrator turns and avoided any append-conflict risk. The plan's "critical-path note" flagged this and the dispatch honored it.
- **Wave 1 parallel Codex EXEC × 3**: three different single-file edits landed in parallel with no conflicts. Combined with Wave 4's batching, the total dispatch count was lower than naively reading the task list would suggest.
- **`dispatched_by` provenance (T5)**: needed by T7; sequencing T5→T7 in Wave 2 meant T7 had the field defined before it tried to use it. No backfill needed.
- **Asymmetric review rule (T7)**: the self-referential policy (Codex doesn't review its own code) landed cleanly. The skip-reason `codex-produced` and `decision_source` field are both present in the failure-class spec.

## What was hard

- **v5.0 lightweight schema**: this bundle was bootstrapped with an experimental minimal schema (fields: `current_phase`, `tasks[]`, `recent_events`) instead of the full v3 standard. The divergence wasn't caught during execution — it surfaced during the 2026-05-22 `doctor --fix` run 6 days later, which injected the 17 missing required fields. Lesson: bootstrap should always derive from the canonical v3 schema template; lightweight variants need a migration path.
- **WORKLOG.md gitignored**: T15's verification target (`grep -q "codex-routing" WORKLOG.md`) passes on the local file but WORKLOG.md is in `.gitignore`. The entry was written; the grep target worked at the time; but the file doesn't travel with the repo. This is by design (local-handoff surface only) but the plan's verification step assumed it would be checkable in git history.
- **Wave 4 parallel-group events**: T9–T12 share `commit: c94b5cb` with Wave 1's T9–T12 timestamp at 16:10Z — the batched-dispatch timing made wave attribution in events.jsonl ambiguous (wave 1 and wave 4 complete events at the same timestamp). Low practical impact but worth noting for future wave-batching patterns.

## Metrics

| Metric | Value |
|---|---|
| Total tasks | 15 |
| Codex-dispatched tasks | 11 (T1–T5, T7, T9–T12) |
| Inline-Claude tasks | 4 (T6, T8, T13–T15) |
| Codex dispatch rate | 73% (target: maximize; plan annotated all eligible single-file tasks `**Codex:** ok`) |
| Wave-mode Codex review | Not applicable (Wave 4 members are append-only; asymmetric rule exempts codex-produced tasks from self-review) |
| Schema gap found in retro | 17 missing v3 fields (fixed 2026-05-22) |

## Key decisions

1. **Aggressive B2 annotation default**: flipped from "add `**Codex:** ok` when obviously well-suited" to "default ok for all single-file edits; mark no only for multi-file/ambiguous." Applied immediately to this bundle's own plan (retrospective validation: all 11 Codex tasks completed without issues).
2. **N-per-wave granularity for review (B1)**: one Codex REVIEW call per wave member rather than one per wave. Increases review resolution; the asymmetric rule keeps this from becoming recursive when the member itself was Codex-dispatched.
3. **Asymmetric review (B3)**: Codex doesn't review Codex-produced output. Decision rationale: same-tool review has known blind spots; inline-Claude or a human review pass adds more value. Preserved as a `review→SKIP(codex-produced)` event with `decision_source: codex-produced`.

---

*Auto-generated by `/masterplan doctor --fix` 2026-05-22. Bundle archived.*
