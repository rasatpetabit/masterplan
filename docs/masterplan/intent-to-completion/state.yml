---
schema_version: 9
slug: intent-to-completion
status: in-progress
phase: brainstorm
topic: "Enhance masterplan so it does a better job of interviewing the operator to understand the true intent of the requested feature. The goal isn't to have the operator tell the model exactly what to it, it's to help the model understand the operators' intentions, so it can design its own solution. We currently don't do a good job of this, and our --complexity=high mode isn't nearly high enough in terms of getting the system to ask probing questions until it has a true understanding. We're also poor at recording those \"goals\", our current goals implementation gathers a large list of specific items which may or may not be helpful, but it doesn't make much effort to try and capture the true \"intent\". And lastly, we're particularly bad about the end of the project, driving the task to completion, and making sure it is ACTUALLY deployed and completed at the end of the work."
complexity: high
complexity_source: null
autonomy: null
planning_mode: auto
created_at: 2026-09-02T21:41:09.790Z
predecessor_transcript: null
spec_path: /srv/dev/ras/masterplan/docs/masterplan/intent-to-completion/spec.md
plan_path: /srv/dev/ras/masterplan/docs/masterplan/intent-to-completion/plan.md
plan_index_path: /srv/dev/ras/masterplan/docs/masterplan/intent-to-completion/plan.index.json
tasks: []
active_run: null
pending_gate: null
goals_enabled: true
goals: [{"id":"G1","text":"The interview asks about intent, exits only through a named on-disk state, and checks for overlapping runs before it starts","signal":"test","evidence":"Will exist on the branch: the sequencer's interview section and seed-time overlap step; agents/mp-intent-critic.md registered for Claude and Pi; test/interview-ledger-resume.test.mjs proving cap refusal, floor, the converged/exhausted/critic_off terminal states, critic payload persistence, and resume from disk; test/overlap-sequencer.test.mjs proving the overlap_review first event and the runs-list topic/goals/planned_paths/worktree fields."},{"id":"G2","text":"Goals record intent, not a checklist","signal":"test","evidence":"Will exist on the branch: test/goals.test.mjs v2 cases (Intent block parsed and hashed, 3-5 outcome goals enforced, signal/evidence optional, v1 still parses, intent amendment re-arms the spec gate); agents/mp-goal-assessor.md returning an intent verdict in the final assessment; the wave summary quoting the outcome line."},{"id":"G3","text":"A run archives as complete only after its declared definition of done ran on a base containing its branch, the live check passed, and the operator confirmed intent","signal":"test","evidence":"Will exist on the branch: test/finish-replay, test/deploy-commit-identity, and test/final-check suites proving deploy_step_started receipts, exit-status check probes, the no_definition_of_done / deploy_indeterminate / deploy_failed gates, the merged-base ancestry checks, the separate completion_confirmed and incomplete_authorized authorizations, incomplete archives for keep/discard/skips, and intent_confirm opening only after a final receipt bound to the deploy base."},{"id":"G4","text":"The knob guard fails the suite on any control surface that lacks a two-value behavioral contract, and complexity and autonomy resolve from the config hierarchy with validation","signal":"test","evidence":"Will exist on the branch: test/config.test.mjs (CLI > repo > user > default, nested whole-object replacement, done repo-local only and schema-validated, malformed YAML fails, invalid enums fail, derived *_source, full and auto_compact aliases warn), test/knob-inventory and test/knob-contract (every control surface has a two-value behavioral test or a closed metadata exemption, including the read-but-ignored synthetic case); the audit's dead flags and fields removed; README and SKILL.md describing the hierarchy and levels table."},{"id":"G5","text":"Gates report measured context usage where the harness exposes it and an explicit unknown state where it does not, and a post-compaction brief exists","signal":"command","evidence":"Will exist on the branch: `mp context-status` returning tokens_at_last_request, appended_est, window, and one of the current / post-compaction / malformed / unsupported states (test/context-status-session-lineage); the turn-close context line with the /compact recommendation above threshold; `mp resume-brief --repo-root` resolving zero, one, or several active bundles; the resume-brief-hook doctor check."},{"id":"G6","text":"v10.0.0 is released, tagged, pushed, and installed into both running surfaces before this run archives","signal":"command","evidence":"Before the goal check (Pi surface): `node bin/install-pi.mjs --check` prints check_ok with version 10.0.0; `git rev-parse -q --verify refs/tags/v10.0.0` exits 0 at the branch tip; `git ls-remote --exit-code --tags origin refs/tags/v10.0.0` exits 0; bootstrap_step events on this bundle record each command. Before archive (Claude surface, at the held branch_finish gate): bootstrap_step events record the merge to main, the push, and the operator's /plugin update with `mp version` from the plugin cache printing v10.0.0 and `node bin/doctor.mjs --only=plugin-registry-drift` clean."}]
review: {"adversary":true}
render: {"images":"off"}
dispatch: {"fabric":true}
goals_md_hash: sha256:2daf01cb4c7a1fd2606efad6257b6c7b6ef0cd0424361394167cf9c5b4e2a1ed
